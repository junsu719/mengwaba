---
// 訂閱降為次要選項(2026-09-01 二輪拍板,見 DECISIONS.md):訂閱要求使用者讓一個陌生網站
// 永久寫進自己的日曆,對還沒有外部連結、沒有信任基礎的新網域而言心理成本太高;下載是零信任
// 成本的主要動作,訂閱留給已經信任本站的人。指向跨年度合併檔(subscribe.ics.ts)而不是單一
// 年度檔——單一年度檔訂閱起來名不副實,隔年加新年度資料不會自動出現在訂閱者的行事曆裡。
const httpsUrl = new URL('/calendar/subscribe.ics', Astro.site ?? Astro.url).href;
const webcalUrl = httpsUrl.replace(/^https?:/, 'webcal:');
---

<details class="ics-subscribe">
  <summary>進階:訂閱(涵蓋所有年度,資料更新後自動同步)</summary>
  <p>
    <a href={webcalUrl}>訂閱到 iPhone / Mac 行事曆</a>
    ,點選後系統會直接跳出「訂閱行事曆」對話框。
  </p>
  <p>
    <strong>Google 日曆</strong>:網頁版左側「其他日曆」旁的 <code>+</code> →「透過網址」,貼上以下網址訂閱:
  </p>
  <p><code class="ics-url">{httpsUrl}</code></p>
</details>

<style>
  .ics-subscribe {
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.75rem 1rem;
  }
  .ics-subscribe summary {
    cursor: pointer;
    font-weight: 600;
  }
  .ics-subscribe p:last-child {
    margin-bottom: 0;
  }
  .ics-url {
    display: block;
    padding: 0.4rem 0.6rem;
    background: #f7f7f7;
    border: 1px solid var(--border);
    border-radius: 4px;
    overflow-wrap: break-word;
    font-size: 0.9rem;
  }
</style>
