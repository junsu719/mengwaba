# 決策記錄

格式:日期 | 決策 | 理由 | 層級(自主/Jun 拍板/跳過關卡)

---

2026-07-08 | 專案任務分級定為 L2,並以 Jun 事先撰寫的 trash-pseo-spec.md(含 §11.5 成功指標與放棄條件)取代正式 /kickoff 流程 | Jun 在對話開場已明確提供完整規格書並確認 §11.5 為驗收標準,等同完成 kickoff 應確認的核心事項(問題、變現、成功指標、放棄條件);另建正式 CLAUDE.md「已確認目標」區塊落實鐵律 9 | Jun 拍板

2026-07-08 | 系統 python3 為 3.9.6,不符 spec 要求的 3.12,改用 `brew install python@3.12` 安裝並以 `/opt/homebrew/bin/python3.12 -m venv .venv` 建立專案虛擬環境 | spec §3 明定 Python 3.12;避免用系統版本导致未來套件相容性問題 | 自主

2026-07-08 | Phase 0 完成:目錄骨架、CLAUDE.md、DECISIONS.md、.env.example、.gitignore、scripts/notify.py(Telegram 通知模組)| 依 spec §9 Phase 0 範圍執行 | 自主

2026-07-08 | Phase 0 完成,Phase 1 待下次 session 開始 | Jun 今日休息,收尾動作(git commit、claude-bible 範本、環境地圖登記)已完成後結束本次 session | Jun 拍板

2026-07-09 | Phase 1 高雄市資料來源:改用高雄市開放資料平台(data.kcg.gov.tw)的全市單一 CSV 資源(resource GUID a6ba725a-488c-4d40-b5a2-c2fe65d3e134,`https://data.kcg.gov.tw/File/DirectDownload/{guid}`),不採用 spec §4 原訂優先序 | 逐一查證結果:①spec 列的來源#1(data.gov.tw/dataset/25888)實際是桃園市資料集且已下架,同平台高雄市對應的 dataset/98147 也已下架,均不可用;②來源#2(環境部 hwms.moenv.gov.tw)頁面有明顯反爬蟲 JS 挑戰腳本,一般 httpx 請求無法取得實際內容,且該防護本身即暗示不歡迎程式化抓取;③來源#3(高雄市開放資料平台)有結構完整、欄位與 spec §6 吻合的全市 CSV(19,038 筆),採用政府資料開放授權條款第一版(明文允許商業利用),但站台 robots.txt 內容為 `User-agent: * / Disallow: /`,僅放行 Googlebot/Bingbot/GPTBot 等具名搜尋引擎爬蟲。已逐一排查該站是否有獨立於網頁爬蟲限制之外的專屬 API/檔案 endpoint(檢查資源頁「API」分頁、/Guid 導覽頁、/About 頁、sitemap.xml、是否存在 api.kcg.gov.tw 等子網域、第三方鏡像 odportal.tw 是否自行代管檔案),結果:該站僅有 data.kcg.gov.tw 單一網域,/Json/Get/{guid} 與 /File/DirectDownload/{guid} 皆與網頁本體同網域、同 robots 政策,無獨立豁免的機器存取端點;odportal.tw 雖自身 robots.txt 開放,但其登記的資源下載連結仍指向 data.kcg.gov.tw,無法繞開。與 Jun 討論後拍板:確認無乾淨的合規 API 路徑後,採低頻善意抓取(User-Agent 誠實標明專案名稱「trash-pseo」與聯絡 email junsu578@gmail.com、單一 citywide 檔案、節流 ≥2 秒、每日僅抓一次),非大量頁面爬蟲,且資料本身合法開放商用,作為風險與效益間的折衷 | Jun 拍板

2026-07-09 | Phase 1 驗收通過(Jun 人工核對自家巷口清運時間無誤)。同時拍板:座標超出台灣範圍(L2 檢出,109 筆/0.62%)的清運點,資料層維持原值不竄改,但 Astro 頁面層不得為該點輸出 geo 座標相關 JSON-LD(如 GeoCoordinates),只顯示地址文字 | 錯誤地理標記(如頁面聲稱清運點在境外)若被 Google 判定為結構化資料造假,可能損害整站 SEO 信譽;不竄改資料層是為保留原始資料可追溯性(鐵律2 絕不填假值),問題留在頁面呈現層解決。已同步寫入 CLAUDE.md 鐵律 6 | Jun 拍板

2026-07-09 | Phase 2 進度:Astro 站台與高雄市全部頁面(首頁、縣市頁、35 個行政區頁、18,805 個清運點頁,共 18,842 頁)已完成並本地 build 成功(6 秒內,含 sitemap 分片 dist/sitemap-0.xml、sitemap-1.xml),JSON-LD 條件式規則(鐵律6)已驗證正確運作。**卡在 Lighthouse 自動化驗收**:本機(Mac mini)未安裝 Chrome/Chromium(僅有 Safari),Lighthouse 依賴 Chrome DevTools Protocol 無法用 Safari 執行。與 Jun 討論後,Jun 選擇下次 session 前自行安裝 Chrome,屆時採「安裝 headless Chromium(`npx playwright install chromium`)執行自動化 Lighthouse」方式恢復驗收,本次 session 到此為止,不強行安裝瀏覽器 | Jun 拍板

2026-07-09 | 額外發現(非本專案程式碼問題):`~/.npm/_cacache` 內有 root 擁有的殘留檔案,導致一般權限 `npm install`/`npx` 報 EACCES。Phase 2 開發時以 `npm install/npx --cache <暫存路徑>` 繞過,未動使用者全域 npm 快取 | sudo 相關的全域環境修復(`chown`)屬影響本機其他專案的變更,不在未經明確同意下執行;已記入 CLAUDE.md 已知問題,留待 Jun 自行處理或明確同意後再修 | 自主(繞過方式)+ Jun 待決(是否執行 chown)

2026-07-10 | Phase 2 Lighthouse 驗收:Jun 已裝好 Chrome,改用「系統 Chrome + `--headless=new`」執行 `npx lighthouse`(未再走原訂的 playwright chromium 安裝路線,因系統 Chrome 已可直接指定 `CHROME_PATH` 使用,更省一次下載);同樣以 `--cache <暫存路徑>` 繞過已知的 npm EACCES 問題。對首頁、縣市頁、行政區頁、清運點頁 4 種代表性頁面各跑一次,發現 SEO 分數卡在 92,查出是 `public/robots.txt` 寫死相對路徑 `Sitemap: /sitemap-index.xml`(不符合 robots.txt 規範要求絕對 URL)。改為 `src/pages/robots.txt.ts` 動態端點,讀取 astro.config.mjs 既有的 `site` 設定值(Phase 2 為 `localhost:4321` 佔位值,Phase 3 換上真實網域後自動同步,不需再改),移除靜態 `public/robots.txt`。修正後重新 build + 重跑 4 頁 Lighthouse,四項分類(Performance/Accessibility/Best Practices/SEO)四頁皆滿分 100 | robots.txt 的 Sitemap 欄位規範要求絕對 URL 是既有標準,非本專案特例;用動態端點串接 site 設定值,避免鐵律4(未申請網域不得寫死)與 SEO 正確性兩者衝突,一次修正、Phase 3 換網域時不必再回頭改 | 自主

2026-07-10 | Phase 3a(技術驗證階段,不含自訂網域與 GSC):網域策略拍板為「先用 Cloudflare Pages 配發的 `trash-pseo.pages.dev` 做技術驗證,自有網域確定後再換」。安裝 wrangler 4.109.0(同樣以 `--cache <暫存路徑>` 繞過 EACCES),Jun 自行完成 `wrangler login` OAuth 授權(帳號 junsu578@gmail.com),建立 Cloudflare Pages 專案 `trash-pseo`,以 `SITE_URL=https://trash-pseo.pages.dev` 重新 build(確保 robots.txt / sitemap 絕對 URL 正確指向線上網域)後用 `wrangler pages deploy dist` 部署 18,848 個檔案。部署後對線上 4 種代表頁重跑 Lighthouse,四項分類四頁皆 100 分,與本地 build 分數一致無落差;確認內部連結、sitemap-index.xml、robots.txt 動態端點在線上環境皆正確指向 `trash-pseo.pages.dev` 絕對網址。**GSC 提交與養站計時延後至 Phase 3b(換上自有網域後)才開始**,本階段不提交 GSC、不綁自訂網域 | 若在 `.pages.dev` 階段就提交 GSC 並開始累積索引與流量歷史,之後換綁自有網域等同網站遷移,會損失既有的 SEO 訊號與索引進度,需重新起算;先在免費子網域做完整技術驗證(部署流程、線上效能、SEO 基本分)可以及早抓出環境落差,同時把「養站計時」留到最終網域才開始,避免兩次搬遷 | Jun 拍板

2026-07-13 | 專案解凍,併入新站群品牌「悶蛙吧 MengWaBa」(正式網域 `mengwaba.com`,Jun 確認已註冊持有,符合鐵律4)。市場重估後改採「單一 domain + 子目錄」工具站群策略:垃圾車主題(本專案)負責開疆,之後陸續加入勞動權益計算機等高 RPM 主題共享同一 domain 的 SEO 信任。執行內容:①站台改為子目錄架構,原 `/{city}/{district}/{point}` 全部移至 `/trash/{city}/{district}/{point}`(`site/src/pages/[city]/` 整組 `git mv` 至 `site/src/pages/trash/[city]/`,原 `index.astro` 一併移至 `site/src/pages/trash/index.astro`);②`site/src/pages/index.astro` 新建為品牌著陸頁,說明悶蛙吧為台灣生活工具站,列出目前工具(垃圾車查詢,連向 `/trash/`)並預留未來工具位置(勞動權益計算機,標示籌備中);③`astro.config.mjs` 的 `site` 預設值改為 `https://mengwaba.com`(原為 Phase 2 佔位值 `localhost:4321`,本地開發仍可用 `SITE_URL` 環境變數覆寫);④sitemap(`@astrojs/sitemap` 依實際檔案路徑自動產生,無需額外設定)、`robots.txt.ts` 動態端點、各頁 breadcrumb JSON-LD、內部連結,皆已同步改為 `/trash/` 前綴與 `mengwaba.com` 絕對網址,並發現 `BaseLayout.astro` 原本寫死的高雄市資料來源頁尾在品牌首頁會誤植,改為具名 slot(`slot="footer"`)讓品牌首頁覆寫為通用文案,垃圾車頁面維持原頁尾不變;本地 `npm run build`(18,843 頁,含新增品牌首頁)與 `npm run preview` 驗證首頁/工具首頁/縣市頁/行政區頁/清運點頁 5 種頁面 HTTP 200、sitemap-index.xml 與 robots.txt 內容皆指向 `mengwaba.com/trash/...`。**尚未部署**,待 Jun 本地驗收後才 `wrangler pages deploy` 或處理 Phase 3b 網域綁定 | Cloudflare Pages 部署成本仍為零,子目錄架構不影響既有 lighthouse 100 分結果(路由深度增加一層,不影響靜態輸出的效能與可存取性分數);既有的成功指標、放棄條件、鐵律(CLAUDE.md)不受影響,僅網域與 URL 結構改變,故不視為新專案、不重跑 /kickoff | Jun 拍板

2026-07-13 | 站群改造驗收通過後,Jun 追加需求:`/trash/` 與 `/trash/{city}/` 頁面加入純前端即時搜尋,輸入路名/里名/清運點名稱可直接找到清運點頁,且不能拖慢首屏、不能影響既有階層連結與 SEO、無 JS 時搜尋框需消失。實作:①新增 `site/src/lib/search.ts` 的 `buildSearchIndex()`,把 city/district 名稱各自存成字典陣列(city 目前僅 1 筆、district 35 筆),`points` 陣列內每筆只存字典索引(數字)+ point_name + village + pointSlug,不存 address 欄位——實測 18,805 筆 publishable 資料的 `address` 100% 等於 `city+district+point_name` 字串相加(全量驗證,無例外),完全可由其他欄位重建,省下逐筆重複存整串地址的空間;②新增兩個 build-time JSON 端點:`site/src/pages/trash/search-index.json.ts`(跨全站,目前僅高雄市)與 `site/src/pages/trash/[city]/search-index.json.ts`(單一縣市範圍),實測輸出檔案皆為 960KB(gzip 後 192KB),相較 12.4MB 的 normalized 原始資料大幅精簡;③新增 `site/src/components/SearchBox.astro`:輸入框以 `hidden` 屬性預設隱藏,`<script>` 執行後才移除 hidden 顯示(無 JS 環境下永遠維持 hidden,不佔版面、不出現在畫面上,爬蟲與一般使用者體驗不受影響);JSON 索引改為「使用者第一次輸入時才 fetch」(非頁面載入就抓),避免大檔案拖慢首屏效能指標;比對邏輯用不分大小寫的子字串 `indexOf` 對 point_name+village+district 組合字串,滿足「和平」能找到「和平一路」的模糊比對需求,結果依匹配位置排序、上限 20 筆;④搜尋框僅加在 `/trash/` 與 `/trash/{city}/` 兩個入口頁,縣市/行政區/清運點頁的既有階層連結與麵包屑結構完全不變。驗收:本地 build 後對 5 種代表頁(品牌首頁/垃圾車工具首頁/縣市頁/行政區頁/清運點頁)重跑 Lighthouse,首次測試發現品牌首頁 accessibility 掉到 91 分——追查是「勞動權益計算機(籌備中)」卡片用 `opacity:0.6` 淡化整塊文字,實際渲染色與白底混合後對比度剩 2.75:1(WCAG AA 需 ≥4.5:1),改用純色 `var(--muted)` 文字 + 淺灰底 `#f7f7f7` 呈現「籌備中」狀態(不倚賴 opacity 混色),重新計算對比度達 6.3:1 後 4 項分類 5 頁皆恢復 100 分 | 索引採字典編碼而非直接存文字,是本次任務「精簡搜尋索引」要求下最直接有效的省空間手段,附帶效益是未來 Phase 4 擴充多縣市時,city/district 名稱依然只需存一份;fetch 延後到使用者輸入時才觸發,是在「不可整包丟給瀏覽器」與「使用者仍要能搜尋」兩個要求間最單純的折衷,不需要額外做分片延遲載入等更複雜的方案;opacity 淡化文字容易在不知情下造成對比度不足,之後若有其他「未上線/籌備中」狀態的 UI 都應比照改用純色文字,不要用 opacity 處理整塊含文字的元素 | 自主(待 Jun 手機驗收)

2026-07-13 | 手機驗收通過後執行部署:`npm run build`(`SITE_URL` 未設定,採 astro.config.mjs 預設值 `https://mengwaba.com`)產出 18,843 頁後 `wrangler pages deploy dist --project-name=trash-pseo` 上傳 18,849 個檔案成功,線上網址暫為 `trash-pseo.pages.dev`(production alias)。對線上 5 種代表頁重跑 Lighthouse,四項分類五頁皆 100 分,與本地一致;確認線上 robots.txt / sitemap-index.xml 內容正確指向 `mengwaba.com`(因為網域是 build 時就寫死進靜態檔案,不受目前用哪個網址存取影響);確認 `/trash/search-index.json`、`/trash/kaohsiung/search-index.json` 線上皆可正確 fetch(HTTP 200、`content-type: application/json`),模擬「和平」查詢比對到 109 筆、結果連結可正常開啟。**自訂網域綁定卡住**:呼叫 Cloudflare API 對 Pages 專案送出 `mengwaba.com` 綁定請求成功(zone 已存在於同帳號下,zone_tag 已取得),但網域狀態卡在 `pending`、`CNAME record not set`——嘗試直接呼叫 zone 的 `dns_records` API 幫忙建立 CNAME 時遭 Cloudflare 回傳 Authentication error(code 10000);检查 `wrangler whoami` 列出的 OAuth token 權限範圍只有「zone (read)」,沒有 DNS 編輯權限,因此無法用現有憑證自動建立所需的 CNAME 記錄。**待 Jun 手動處理**:登入 Cloudflare Dashboard → mengwaba.com 這個 zone → DNS → 新增紀錄:類型 CNAME、名稱 `@`(根網域)、目標 `trash-pseo.pages.dev`、Proxy 狀態設為 Proxied(橘雲);建好後 Cloudflare Pages 通常幾分鐘內會自動驗證並簽發 SSL 憑證轉為 active,不需要再跑 wrangler。GSC 提交依 Jun 指示留待網域正式綁定並由 Jun 確認線上版本後才進行 | 未擅自嘗試取得更高權限的 API token 來繞過鐵律以外的憑證限制(如重新 `wrangler login` 要求更大 scope),因為 DNS 屬於會影響正式網域解析的共用基礎設施變更,權限不足時應該請 Jun 手動確認或授權,而不是想辦法自己補權限做掉 | 自主(部署與驗證)+ Jun 待辦(手動補 CNAME)

2026-07-13 | 本次 session 收尾確認:①品牌定案為「悶蛙吧 MengWaBa」,網域 `mengwaba.com` 已由 Jun 購入持有;②垃圾車工具(`/trash/` 子目錄架構 + 即時搜尋)已部署上線至 `trash-pseo.pages.dev`,線上 Lighthouse/sitemap/robots.txt/搜尋功能皆驗證無誤,唯自訂網域綁定卡在 CNAME 記錄未設,待 Jun 手動於 Cloudflare Dashboard 新增(見上一條決策的操作步驟);③**下次開工順序**:Jun 設定好 CNAME → 確認 `mengwaba.com` 網域狀態轉為 active(含 SSL 憑證簽發)→ 才提交 Google Search Console(即為養站計時起點)。本次 session 到此結束,不繼續往下做 | 依 Jun 指示明確記錄收尾狀態與下次開工順序,避免下次 session 需要重新爬梳對話記錄才能銜接進度 | Jun 拍板

2026-07-15 | CLAUDE.md 環境規則改為雙機開發:PC WSL2(`~/projects/mengwaba`)可開發、Mac mini M4(`~/projects/trash-pseo`)維持唯一執行環境(pipeline 排程、`wrangler pages deploy` 部署皆只在 Mac mini 執行),程式碼以 GitHub(`junsu719/mengwaba`)同步,每次 session 開始 `git pull`、結束前 `git push` | Jun 新增 PC WSL2 作為第二台開發機,但排程與部署仍應維持單一來源,避免雙機重複部署互相覆蓋 | Jun 拍板

2026-07-16 | Phase 4 啟動,第一個擴充縣市選定臺中市。資料來源查證:①臺中市開放資料平台(opendata.taichung.gov.tw)本身查無該資料集頁面(dataset id 皆 404),改採 Jun 提供的另一路徑 data.gov.tw/dataset/84004「臺中市定時定點垃圾收運地點」,其 API(`https://data.gov.tw/api/v2/rest/dataset/84004`)為 data.gov.tw 平台正式登記的 REST 端點,回傳的 `resourceDownloadUrl` 指向臺中市府自家網域 `newdatacenter.taichung.gov.tw/api/v1/no-auth/resource.download?rid=...`,屬官方註冊之機器可讀 API 資源(非網頁本體),取得方式與高雄「爬網頁 CSV、需繞過 robots.txt 限制」的情況不同,不受高雄案例的 robots.txt 疑慮影響;`newdatacenter.taichung.gov.tw/robots.txt` 實測回傳其後端反向代理的 500 錯誤(`DNS lookup failure for: localhost:10101robots.txt`),判斷為該站未設定 robots.txt 導致代理層處理錯誤,而非刻意封鎖,且此端點本來就是資料開放平台登記給程式化存取用的 API,非一般網頁爬蟲情境。授權比照高雄:license "1"(政府資料開放授權條款第一版,含商業利用)。低頻善意抓取原則(單一請求、User-Agent 標明專案與聯絡方式)比照高雄做法沿用 | 官方登記於 data.gov.tw 平台的 API 端點,合規風險遠低於高雄案例的網頁爬蟲繞過,無需另行拍板,依既有鐵律 3(節流善意抓取)實作即可 | 自主

2026-07-16 | Phase 4 臺中市 fetch → normalize → validate 全流程跑通(20,033 筆原始資料 → 20,004 筆正規化),資料建模與過濾決策:①臺中資料完全無經緯度欄位,故全部 20,004 筆 `lat`/`lng` 皆為 null,頁面層依鐵律 6 既有規則(無座標不得輸出 GeoCoordinates JSON-LD)自然涵蓋,不需新規則;②`task_type` 除「定點」「沿街」外還有「往廠」(車輛返回焚化廠的內部調度紀錄,19 筆,`caption` 恆為「焚化廠號」、全部時間欄位皆空白),另有 10 筆 `task_type` 為沿街/定點但 `caption` 同為「(進)焚化廠號」的同類調度紀錄——這兩類共 29 筆非民眾可查詢的清運地址,正規化時整筆排除(非竄改既有資料,是判斷「不屬於清運點」不予收錄,不違反鐵律 2);③`collection_type` 依 `task_type` 明確區分:「定點」→`定點清運`(語意同高雄既有值)、「沿街」→`沿街收運`(沿路行駛中經過,非固定停留);頁面呈現時「沿街收運」不可比照「定點清運」給出精確停留時刻的語氣,留待該類頁面實際生成時(Phase 4 後續)處理,本次僅完成資料層正確標記;④臺中資料另有「資源回收」(r_d1~r_d7)與「一般垃圾」(g_d1~g_d7)兩组各自獨立的星期/時間欄位,不像高雄只有單一垃圾時刻表。schema 新增可選欄位 `recycling_schedule`(與既有 `schedule` 同形狀),`schedule` 維持只放一般垃圾時刻(對齊原本頁面「垃圾車幾點來」的核心語意與高雄既有消費端),此為向下相容的新增欄位,不影響高雄既有資料與尚未讀取此欄位的頁面層代碼;⑤星期對應驗證:以全體 20,033 筆統計各 d1~d7 欄位空白率,d7(100% 空白)、d3(99.8% 空白)恰為臺中已知的週日、週三公休慣例,確認 d1=週一…d7=週日與 ISO weekday 直接對應,不需轉換。驗證結果:L1 完整率 99.85%(31 筆為來源本身缺漏一般垃圾時段但地址真實存在的個案,如實留 null,未達 5% 中止門檻)、L2 時間/星期格式 0 違規、座標檢查因無座標可查自動略過、L3 首次執行略過比對,三層皆通過。pipeline 三支程式(fetch/normalize/validate)重構為可傳入 city_key 的多縣市版本(`python pipeline/xxx.py taichung`),高雄的既有函式簽名、輸出檔名、`data/meta.json` 既有 `kaohsiung` 條目皆未變動,僅新增 `taichung` 條目。**本次僅完成資料層,未生成 Astro 頁面、未動高雄任何檔案**,待 Jun 驗收正規化 JSON 與驗證報告後才進入頁面生成 | 過濾焚化廠調度紀錄與新增 `recycling_schedule` 皆屬「資料如實呈現、不捏造」原則下的合理判斷,記錄理由供之後其他縣市比對時參考;pipeline 改為多縣市可傳參數的形式是 Phase 4 spec 既定方向(擴充至六都→全台 22 縣市),此時機一併重構可避免之後每個縣市再各自搬一次 | 自主(待 Jun 驗收資料層)

2026-07-16 | 資料層驗收通過後接續完成 Phase 4 頁面層:Astro 頁面生成沿用高雄模板,`site/src/lib/data.ts` 把原本寫死的單一 `CITY_SLUG`/`CITY_NAME` 常數重構為 `CITIES` 註冊表(每個縣市一筆 `{slug, name, file, sourceName, sourceUrl}`),所有頁面路由(縣市/行政區/清運點)、兩個搜尋索引端點皆改為對 `CITIES` 疊代產生,之後新增縣市只需註冊一筆,不用再逐一修改路由檔案;過程中一併挖出並修正舊有程式碼裡多處寫死「高雄市」字樣(FAQ 大型垃圾問答、頁尾資料來源、注意事項段落),改用當下頁面所屬縣市的變數帶入。頁面呈現依前次拍板規則區分沿街收運/定點清運:到站時間與離站時間相同的沿街收運點,措辭改為「約 HH:MM 經過」,定點清運維持原本到站/離站雙欄呈現;有 `recycling_schedule` 資料的點新增「資源回收時刻」區塊,無此資料的點(含全部高雄資料)不顯示。另外發現行政區頁與清運點頁的麵包屑導覽(給使用者看的 `<nav>`)少了「垃圾車清運時間查詢」這一層,與 JSON-LD 的 BreadcrumbList(該結構原本就是完整層級)不一致,一併修正兩處使視覺與結構化資料層級一致。`npm run build` 產出 38,846 頁(高雄 18,805 + 臺中 19,973 + 品牌首頁/工具首頁等),sitemap 自動涵蓋兩縣市所有頁面;本地 dev server 抽測旗津區、中區的行政區頁與清運點頁,麵包屑顯示與 JSON-LD 皆核對一致 | `CITIES` 註冊表是 Phase 4「擴充至六都→全台 22 縣市」既定方向下,趁第二個縣市上線時就該做的重構,避免每加一個縣市都要動一次所有頁面檔案;麵包屑不一致屬於既有程式碼的既存 bug(城市頁本身就有正確三層,行政區/清運點頁少一層是重構前就有的疏漏),藉這次機會一併抓出修正 | Jun 驗收通過

2026-07-16 | CLAUDE.md「執行環境」規則調整:原規則將「pipeline 自動排程」與「wrangler 手動部署」綁在一起,一律限定只能在 Mac mini M4 執行。重新檢視後拆分為兩條不同規則:①**手動部署**(`wrangler pages deploy`)PC WSL2、Mac mini 皆可執行(部署前先 `git pull` 取最新版即可),因為 Cloudflare Pages 是把本機 build 出的靜態檔案上傳到同一個雲端專案,部署結果只取決於上傳的 `dist/` 內容,與從哪台機器執行 `wrangler` 指令無關,沒有理由限制單一機器;②**自動排程**(pipeline 每日 launchd)維持唯一限定 Mac mini M4,PC 不得設定任何自動排程,因為這是兩台機器都可能在背景無人介入的情況下各自觸發的定期行為,若雙機都排程會同時各自抓取、正規化、寫檔,造成 `data/normalized/` 內容互相覆蓋或 git 推送衝突,這個風險與「手動、一次性、有人在場執行」的部署行為性質不同,不能一概而論 | 原規則把部署和排程混為一談,阻擋了「Jun 人在 PC 前想直接部署」這種其實沒有風險的操作;拆分後保留真正需要單一來源的部分(無人值守的自動化),放寬不需要限制的部分(有人手動觸發、當下就能看到結果的一次性操作) | Jun 拍板

2026-07-17 | Phase 4.5 Phase 1 PoC(D1/KV/打包三方案比較)完成後,定案一系列推翻專案最初「Mac mini 24 小時自動化」前提的重大方向決策,共 7 點:①**資料存取採 D1**:KV 免費層每日僅 1,000 次寫入,單一縣市一次批次更新即超標,會逼迫升級付費;D1 免費層每日 10 萬列、批次匯入速度約為 KV 的 10 倍,適合本專案的更新型態;打包進 Functions(選項 A)排除,因為會把資料更新與部署耦合在一起(改資料就要重新 build+deploy,無法解耦),且以目前每縣市資料量估算,4-5 個縣市就會逼近 Workers 打包大小上限。②**hybrid 渲染邊界**:靜態預生成品牌首頁 `/`、工具首頁 `/trash/`、縣市索引頁 `/trash/{city}/`;on-demand D1 查詢行政區頁 `/trash/{city}/{district}/` 與清運點頁 `/trash/{city}/{district}/{point}/`——低量變動少的入口頁維持靜態換取最快首屏與最簡單的 SEO 資產穩定性,高量的行政區/清運點頁改 on-demand 查詢以徹底脫離 Cloudflare Pages 檔案數天花板。③**更新頻率改為季度手動**(最多一季一次,甚至半年一次),非原先設想的每日自動:垃圾車清運路線變動頻率極低,每日抓取只是在浪費額度並增加雙機衝突風險,沒有對應的業務需求支撐每日頻率。④**機器角色脫鉤**:D1 架構讓網站獨立跑在 Cloudflare 邊緣渲染,不再需要 Mac mini 24/7 開機——機器僅在季度推資料時短暫使用,推完可關機;Mac mini 的 24/7 特性保留給未來「機器即產品」型專案(如本地 AI 推理),與 pSEO 這條線脫鉤。⑤**未來高頻更新題材優先用 Cloudflare Cron Triggers**(抓取跑在 Workers、寫入 D1、不需本機開機),而非比照本專案設 Mac mini launchd 排程;但 pSEO 甜蜜點本質是低頻長尾資料,預期極少遇到需要中/高頻更新的題材。⑥**L1/L2/L3 驗證鐵律不變**(驗證失敗絕不推送 D1),且更新頻率越低越要靠驗證擋髒資料,不因低頻而放鬆把關。⑦**移除/簡化原為 24/7 設計的項目**:launchd 每日排程、看門狗、斷網重試、Mac mini 24/7 開機設定皆不再需要;Telegram 通知從「任何腳本失敗必發」的鐵律降級為選配,手動執行流程仍可選用,但不再是每日排程下的必要安全網 | 一開始規劃「Mac mini 24 小時 launchd 每日排程」是假設清運路線資料需要高頻更新,但實際查證高雄、臺中兩縣市的清運路線在 Phase 4 擴充過程中都未觀察到高頻變動,且每日排程本身在雙機開發環境下反而是額外的衝突風險來源(見 2026-07-15 決策);Phase 4.5 為了解決 Cloudflare Pages 20,000 檔案上限問題重新設計資料存取與渲染架構時,一併重新檢視更新頻率的真實需求,發現原本的 24/7 前提是路線規劃初期的過度設計,應該以實際資料變動頻率反推架構,而非反過來 | Jun 拍板

2026-07-20 | **正式上線事故**:依 Phase 4.5 規格首次將高雄+台中的 D1 動態版部署至 production(`mengwaba.com`),流程執行到一半發現部署架構整個不成立,已即時處置(Dashboard rollback 回舊版靜態站),記錄根因、流程失誤與後續影響如下。

**事故經過**:①遠端 D1 資料匯入正常完成(高雄 18,805、台中 19,973 筆,列數與本地一致);②發現 Cloudflare Pages 專案 `trash-pseo` 尚未設定 D1 binding(`POINTS_DB`),Jun 於 Dashboard 手動加上 Production/Preview 兩環境的 binding;③`npm run build` 產出 dist(client 僅 14 檔案,符合 hybrid 架構預期的檔案數大幅下降);④第一次 `wrangler pages deploy dist/client`(未帶 `--branch`)因目前 git 分支非 `main`,誤判為 preview 部署;⑤改帶 `--branch main` 重新部署後,實際成為新的 production 部署(`74da2c04`),取代舊版純靜態站(`f71b85b9`,2026-07-13 上線);⑥curl 驗證 `https://mengwaba.com/trash/kaohsiung/nanzi/00001/`,回傳 HTTP 200 但內容是品牌首頁 HTML,非預期的清運點頁——⑦追查後確認**架構性不相容**,非設定疏漏:`@astrojs/cloudflare` v14.1.3 已是純 Workers 導向 adapter(官方 README 明載「An SSR adapter for use with Cloudflare Workers targets」),build 產出的 `dist/server/wrangler.json` 屬 Cloudflare 新版「Workers with static assets」統一格式(`main: entry.mjs` + `assets: {binding: "ASSETS", directory: "../client"}`),設計上就是要用 `wrangler deploy` 部署成 Worker,不是 `wrangler pages deploy`；實測用 `--config`/`--cwd` 嘗試讓 `wrangler pages deploy` 讀這份 config 皆失敗,最終在嘗試以 `pages_build_output_dir` 補救時得到關鍵錯誤訊息「The name 'ASSETS' is reserved in Pages projects」——Pages 產品本身保留 `ASSETS` 這個 binding 名稱給自己的靜態檔案服務用,和 adapter 自訂的 `ASSETS` binding 直接衝突,證實這條路線在架構上走不通,不是加個設定值就能修好。⑧因此 production 有一段時間(約數分鐘,精確時長未记录)对所有行政區頁與清運點頁回傳錯誤內容(品牌首頁 HTML,HTTP 200 非 5xx,故監控/使用者不易第一時間察覺);⑨立即經 Jun 確認後於 Pages Dashboard 一鍵 rollback 回 `f71b85b9`(2026-07-13 舊版純靜態站),線上恢復正常;⑩台中殘留邊緣快取(rollback 後短暫仍可能被少量使用者看到 74da2c04 版本內容)經 Jun 評估影響極低,不特別處理,留待 Phase 4.5 正式上線時直接覆蓋。

**重大附帶發現**:目前 `main` 分支的程式碼已包含台中的靜態頁面生成邏輯(Phase 4 已完成、Phase 4.5 尚未完全套用前的中間狀態),若單獨對 `main` 分支執行純靜態 `npm run build`,會產生 38,857 個檔案,必然超過 Cloudflare Pages 免費方案 20,000 檔案上限。這代表**在 Phase 4.5(D1 + Workers 部署)真正完成前,`main` 分支已經無法用原本的純靜態 `wrangler pages deploy` 方式部署**——沒有回頭路,Workers 遷移不再是「其中一個可選方案」,而是唯一能讓 `main` 分支繼續上線的路徑。

**流程失誤(與根因分開記錄)**:今天是 Phase 4.5 新架構第一次影響 production 的部署,卻直接對正式網域 `mengwaba.com` 執行,沒有先在非正式環境(如 `*.pages.dev` preview 或未來 Workers 的 `*.workers.dev`)做過 curl 紅線驗證確認架構本身走得通,才切正式網域。若當初先在 preview 網址驗證,就會在觸及 production 之前發現 ASSETS binding 衝突,不會有任何使用者看到錯誤內容。已依此教訓新增 CLAUDE.md 鐵律(見下方對應條目)。

**後續處置**(留待下次 session,今日不繼續部署):evaluate 是否延用付費 Pages Functions 模式繞開(已知不可行,Pages 保留 ASSETS binding 為產品層級限制,非方案),或改走 `wrangler deploy` 部署為 Cloudflare Worker + Workers Custom Domain 取代現有 Pages 專案的網域綁定(初步判斷為唯一可行路徑,`workers.dev` 尚無同名 Worker,無命名衝突;`mengwaba.com` 目前的 Pages Custom Domain 綁定需改綁到 Worker,此為需要 Jun 明確同意的網域路由變更)。phase4.5-hybrid-rendering-spec.md 已同步更新部署階段為 Workers 路徑 + 兩段式驗證流程 | 根因記錄是為了讓下次接手 Workers 遷移的人(可能是未來的自己)不必重新踩一次同樣的坑,尤其「ASSETS 為 Pages 保留字」這個結論得來不易(試了 `--config`、`--cwd`、`pages_build_output_dir` 三種繞法才確認是產品層級硬限制,非我方 config 遺漏);流程失誤獨立於技術根因記錄,是因為就算技術本身沒問題,直接對 production 部署新架構這件事本身就不該發生,兩者要分開檢討才不會下次只顧著修技術面、忽略流程面同樣的疏漏會用不同的技術外殼重演 | Jun 拍板(rollback 決策)+ 自主(事故記錄與根因調查)

2026-07-21 | 高雄市資料來源從 CSV 改為官方 PDF 班表,並訂出三項 PDF 排版瑕疵處理原則。**改用 PDF 原因**:2026-07-09 拍板採用的 data.kcg.gov.tw 全市 CSV(resource guid a6ba725a-488c-4d40-b5a2-c2fe65d3e134,19,038 筆)經檢視後判斷整份很可能是資源回收車路線,和一般垃圾車清運時刻混淆,不應繼續使用或留著誤導後人;改用高雄市政府環境保護局官方 PDF 班表(`kepbgps.kcg.gov.tw/download_schedule.aspx`),垃圾/回收分開標示、結構完整,依行政區各自一份 PDF。該站 robots.txt 禁止自動存取,故不比照臺中/舊高雄寫抓取程式,改由 Jun 每季手動下載 36 份行政區 PDF 至 `data/raw/kaohsiung-pdf/{行政區}.pdf`(那瑪夏區、茂林區官方未提供班表,確認為真的沒有,不得用鄰區資料替代或推測填補);解析核心獨立成 `pipeline/kaohsiung_pdf.py` + `pipeline/parse_kaohsiung_pdf.py`,取代原本 `fetch.py`(CSV 抓取)→`normalize.py`(`normalize_record_kaohsiung`)的路徑,兩處舊程式碼直接刪除、不保留,避免有人誤執行抓回已知錯誤的資料源。**三項 PDF 排版瑕疵處理原則**:①中文字用「同一字疊印兩次、水平偏移約 0.3pt」模擬粗體(數字/符號不受影響),`dedupe_page()` 依相同文字、相同列(`top` 取到小數點後 1 位)、`x0` 相差 <1pt 視為同一字元去重;②少數表格列剛好卡在 PDF 分頁斷點被切成殘缺片段(三民區實測 12/1655=0.72%),一律嚴格用格式驗證篩掉、記錄丟棄原因,絕不用猜測拼接補上缺的欄位——未來若要做跨頁邊界感知拼接,必須是偵測第 N 頁末列與第 N+1 頁首列互為殘缺延續、拼接後仍要通過同一套格式驗證才採用,本次 Phase A/B 尚未實作,列為已知限制留待之後處理;③除三民區、鳳山區外,其餘行政區 PDF 背景都有一個旋轉的浮水印「高雄市政府環境保護局」,字級遠大於表格內文且變換矩陣帶有旋轉分量,會被 pdfplumber 依座標誤塞進疊到的表格儲存格汙染文字(起因是仁武區等區丟棄率一度異常高達 30%+ 才追查出來),`dedupe_page()` 用字元變換矩陣是否有旋轉分量精準濾掉浮水印,不影響表格本身的直式字元。另外桃源區 PDF 多一欄「清運方式」(定點/沿街),其餘 35 區皆無此欄、隱含全部視為定點清運,此假設已用三民區資料實測比對 Jun 的實地資料驗證無誤。最終 36 區解析完成、2 區確認無班表(共 38 區),19,436 筆有效、248 筆丟棄(皆為分頁斷點格式驗證未過,無其他丟棄類型) | 資料真實性優先於資料量或既有 pipeline 的沿用慣性,一旦懷疑資料源本身語意錯誤(回收車當垃圾車用),繼續用只會讓後續所有頁面內容失真,不符合鐵律 2；PDF 排版瑕疵若不逐一排查根因(而是直接調高容錯或放寬驗證門檻),容易把「解析邏輯有洞」誤判成「資料本身有洞」,錯放真正該丟棄的殘缺列,或錯殺其實正常的列(如浮水印曾讓仁武區丟棄率誤判到 30%+) | 自主(待 Jun 驗收正規化結果)

2026-07-21 | `point_id` 產生方式改為「內容雜湊 + 同址多筆消歧對照表」,高雄(新 PDF pipeline)與臺中一併套用。**改動原因**:原本高雄/臺中都是用「資料來源逐列出現順序」編全域序號(`KHH-{區}-{序號}`/`TXG-{區}-{序號}`),PDF 沒有可沿用的穩定序號欄位,若沿用舊做法,官方調整 PDF/CSV 內部排列順序時全部清運點 URL 會跟著整批洗牌;臺中舊制有同樣風險,但因目前 GSC 尚未提交、線上流量為零,改動的實際影響為零,一併在這個時機修正,之後擴充其他縣市直接沿用同一套規則,不必再各自設計。**方案內容**:改用「里別+清運點名稱」文字的內容雜湊(sha256 前 10 hex 字元,40 bits,以單一行政區內筆數規模估計碰撞機率可忽略)當作 `point_id` 的核心,只要地點描述文字不變,ID 就不變,不受列順序、其他點新增/刪除影響;同址多筆班次(同一地址有兩班不同時間的車,非資料錯誤,三民區驗證報告已確認此為真實情況)需要消歧,以一份 git 版控的對照表 `data/id-map/{city_key}.json` 記錄每筆的消歧指紋(車牌+首個抵達時間),下次更新時依序用①車牌完全比對②抵達時間最接近配對既有列③兩者皆缺時依原始順序 position-pair④真正新出現的列才分配新的消歧序號(接續既有筆數,不重用),確保未變動的清運點跨季更新後 URL 不變;既有對照表中未被配對到的舊條目刻意保留不刪除,避免同址班次未來重新出現時消歧序號被重新分配。`data/id-map/{city_key}.json` 需要 git 追蹤,遺失即無法保證跨季 URL 穩定 | 內容雜湊比全域序號更能反映清運點本身的身分(地點不變、ID 就不變),避免每次官方資料重新排列就讓所有已索引的 URL 失效、SEO 從頭來過;消歧對照表是唯一能在「同址多班次」與「跨季 URL 穩定」兩個要求間兼顧的做法,單純雜湊「里別+清運點名稱」在同址多筆情境下會產生碰撞 | 自主

2026-07-22 | point_id 改內容雜湊方案(2026-07-21 決策)後,發現 `site/src/lib/data.ts` 的 `parsePointId()` 與 `pipeline/push_d1.py` 的 `parse_district_slug()` 都還寫死 `\d+` 只吃全數字序號,新雜湊 ID 因含 A-F 字母,高雄/臺中各約 99% 的 point_id 解析失敗——足以讓幾乎每個行政區頁/清運點頁 500,或讓 D1 匯入直接中止。**這個漏洞是靜態比對(逐一列出六個呼叫點、實測全量資料跑 `parsePointId`)才抓到,不是動態測試發現的**:本地 `astro dev` 當時綁定的 D1 還是 07-17 的舊序號舊資料,新舊 ID 從未真正一起跑過同一條路徑,動態測試完全沒機會觸發這個問題。修復與後續驗證共歸納出三條原則,供之後擴充其他縣市、或再次調整 ID/schema 方案時對照:①**改 ID 方案時必須連帶盤點所有下游消費端**,不能只改產生端(pipeline)就視為完成——本次若不是逐一列出 `groupByDistrict`、`[point].astro`(主查找/鄰近清運點)、`index.astro`(清運點列表/時段總表)、`search.ts` 六個呼叫點並實測全量資料,這個 bug 會一路帶到部署後才被使用者觸發;②**point_id 對下游必須維持不透明,只當字串用**(分組鍵、URL 片段、字串相等比對),不對內容字元集做任何假設——這也是為什麼修復採用依 `-` 結構切分而非用 `[0-9A-Z]+` 這種只是擴充字元集、下次規則一變照樣會炸的 regex,目標是徹底不對 ID 內容做假設;③**面向使用者的展示頁,單筆資料格式異常絕不能讓整頁 500**——`parsePointId()` 解析失敗一律回傳 `null` 而非 `throw`,六個呼叫點各自 fallback(視同不比對自然落入既有 404、連結退化為純文字、該筆從分組或搜尋索引略過),使用者頂多看到某一筆連結壞掉,不會整頁掛掉。三項原則已用實際資料驗證(全量 39,440 筆 point_id 100% 解析成功、故意餵入異常格式確認優雅降級、`astro build` 與 workers.dev 動態測試皆通過) | 這條教訓與 2026-07-20 的部署事故同一個根因類型:改動只在「自己這一層」驗證通過,沒有沿著資料實際流經的完整路徑(這次是 pipeline 產生端 → 網站消費端)逐一確認,兩次都是「看似驗證過但沒測到真實路徑」;把檢查清單精神記下來,是為了下次改 ID/schema 這類橫跨 pipeline 與網站兩層的方案時,能主動照這三條走一遍,而不是等使用者回報 500 才發現漏了哪一段 | 自主(待 Jun 確認 mengwaba.com 切換前的最終驗收)

2026-07-22 | **`mengwaba.com` 從 Cloudflare Pages 切換到 Workers 的完整事故與根因**(D3,最終切換成功)。**根因**:apex `mengwaba.com` 上有一筆 2026-07-13 手動建立的 CNAME 記錄(`@ → trash-pseo.pages.dev`,proxied,見同日決策條目),這筆記錄是直接透過 DNS API 建立、不屬於任何 Cloudflare Custom Domain 系統自動管理,因此被 Workers Custom Domain 判定為「externally managed」(錯誤碼 100117:`Hostname 'mengwaba.com' already has externally managed DNS records (A, CNAME, etc). Delete them first or try a different hostname.`)——即使 wrangler 內部請求已經預設帶上 `override_scope`/`override_existing_origin`/`override_existing_dns_record` 三個旗標全部 `true`,Cloudflare 仍然硬性拒絕,無任何 override 參數可繞過,只能刪除或換 hostname。這代表 Pages Custom Domain(高層抽象)與這筆 DNS record(底層原始記錄)是兩個獨立的層級——移除/重掛 Pages Custom Domain 完全不會動到底層 CNAME,所以第一次嘗試「先移除 Pages、再掛 Worker」時,不管等多久、重試幾次都不可能成功(第一次事故約斷線 26 分鐘;第二次改寫自動化輪詢腳本、60 次重試滿 5 分鐘仍全數失敗、自動回滾,斷線約 14 分 41 秒)——因為卡住的不是「Pages 釋放的傳播延遲」,是這筆完全不同層級的殘留記錄,重試次數再多也無效。**解法**:①用一個從未使用過的子網域(`cutover-test.mengwaba.com`)零風險驗證 Worker Custom Domain 機制本身沒問題(確實掛得上、確實 200,清理後確認乾淨移除)——排除「我方權限/設定有問題」這個可能性;②在完全不動 Pages 的前提下,直接對 `mengwaba.com` 嘗試掛載並開 `WRANGLER_LOG_SANITIZE=false` 抓下未過濾的原始錯誤 JSON,才第一次真正看到 `externally managed` 這句關鍵字(先前的嘗試只記錄 exit code,錯過了診斷用的關鍵資訊);③向 Jun 申請一個具備 `Zone > DNS > Edit` 權限的新 API token(存進 `.env` 的 `CLOUDFLARE_API_TOKEN`,已確認 `.gitignore` 排除);④用新 token 讀取並完整備份那筆 CNAME 的全部欄位(type/name/content/proxied/ttl,record id `b584f2148278629a8030c063a104240a`),核對與預期一致後才刪除;⑤刪除後立即掛 Worker Custom Domain,`workers/domains` API 確認 `enabled: true` 且已核發新的 `cert_id`,輪詢第一次就回 200,沒有觸發任何 rollback;⑥切換後全面複測正式網域(首頁、工具首頁、兩個城市索引頁、多個新雜湊 point_id 清運點頁含旗津同址消歧兩筆、search-index.json)皆 200,標題內容正確。**教訓與可複用結論**:①「Custom Domain」(Pages/Workers 的高層抽象)與該 hostname 的原始 DNS record 是兩個獨立層級,移除/重掛前者不會影響後者,診斷卡住的問題時要優先確認是哪一層卡住,不能只盯著高層抽象反覆重試;②API 回應 `success:true` 不代表操作真的生效,尤其是 `result` 為空陣列時,一律要用對應的列表端點(如 `workers/domains` GET)實際查詢確認,不能只信任單次寫入請求的回應本身;③重試次數/等待時間對「傳播延遲」有效,對「結構性阻擋」完全無效——分不清楚是哪一種,重試預算設得再寬也只是拖長斷線時間,應該儘早捕捉完整原始錯誤訊息(關掉 log 過濾、印出原始 JSON)來判斷根因類型,而不是先假設是傳播延遲就一路重試;④切換一個 hostname 的代管產品前,先在拋棄式子網域上驗證新產品的掛載機制本身沒問題,能把「機制問題」和「hostname 特定狀態問題」這兩種完全不同的根因徹底分開,是本次能快速定位根因的關鍵一步,值得列為未來任何「網域代管產品搬遷」的標準前置動作;⑤刪除任何 DNS 記錄前必須先完整備份全部欄位並核對與預期一致,且要先確認「重建路徑」本身可行(本次是 Pages Custom Domain 的 POST 重新綁定,已在事故過程中反覆驗證好幾次確實有效、且通常在一分鐘內恢復),備份跟重建路徑兩者缺一都不該執行刪除 | 這次能在同一天內就從「完全不明白 409 卡在哪」走到「精準定位並乾淨解決」,關鍵在於堅持用唯讀查詢、拋棄式子網域測試、完整原始錯誤訊息這幾個「先確認再動手」的方法,而不是靠反覆重試撞運氣;把這套方法記下來,是因為本站之後還有 Phase 4.5 全台擴充、未來也可能有其他網域搬遷情境,同一套「先唯讀盤點、拋棄式環境驗證、抓完整錯誤、備份後再刪」的流程應該直接複用,不必每次都重新摸索 | Jun 拍板(核准實際切換)+ 自主(診斷、根因調查、執行)

2026-07-23 | 桃園市資料源判斷:確認 `data.tycg.gov.tw`(舊平台)已死、`opendata.tycg.gov.tw`(新平台)無法自動化存取,改用桃園市政府環境管理處「垃圾清運路線即時查詢系統」(`route.tyoem.gov.tw`)的內部查詢端點作為主要資料源。**判斷過程**:①`data.tycg.gov.tw` 用 Google 公開 DNS(`dns.google/resolve`)查詢回傳 `Status:3`(NXDOMAIN),網域本身已不存在,不是 API 退役而是整個平台消失;②國家資料集登錄平台 `data.gov.tw/dataset/25888`(這份「垃圾車清運點資訊」資料集的權威登記)明寫「資料集已下架,此為歷史資料留存」,最後更新 2024-10-18(近 21 個月前),頁面未提供任何替代資料集連結;③現行平台 `opendata.tycg.gov.tw` 本身存活且持續維護(搜得 2026-04-16 更新公告),但站台被 WAF 主動阻擋自動化存取——連 `robots.txt` 本身都讀不到(TLS 交握成功、請求完整送出後遭靜默丟棄,或偶爾回一頁明確的「Request Rejected」阻擋頁),從本機與 Anthropic WebFetch 兩條獨立網路路徑測試結果一致,且站內搜尋(`site:opendata.tycg.gov.tw` + 垃圾車/清運/收運關鍵字)找不到這份資料集被搬遷過去的痕跡,判斷是資料集本身停止維護而非搬家。**授權依據**:`route.tyoem.gov.tw` 頁尾指向 `www.tycg.gov.tw/cp.aspx?n=5847&s=1314`(政府網站資料開放宣告),該頁本身被 HiNet Anti-DDoS JS 挑戰頁擋下(要求執行 JS 才放行,任何非瀏覽器 client 讀到的都是同一張「請稍候」頁),改用 Wayback Machine 讀取歷史存檔(2025-01-13/04-24/09-05 三個時間點內容一致)取得條款全文:採**政府資料開放授權條款第 1 版**,以無償、非專屬方式允許不限時間地域之重製、改作、編輯、公開傳輸,明文涵蓋「開發各種產品或服務(加值衍生物)」,唯需標示出處,此授權適用於「桃園市政府網站上刊載之所有資料與素材」,`tyoem.tycg.gov.tw`(環管處子站)頁尾也連到同一份宣告,視為該站已援引適用。**次要來源**:過程中另找到環管處公告的《桃園市各區清潔中隊家戶垃圾定點收受點一覽表》PDF(`tyoem.tycg.gov.tw`,最新版本 1150416),但實測其「收受時間」欄位是長時段開放窗口(如 07:00~22:00)而非逐點時刻表,且僅涵蓋全市 12 個行政區的定點丟渣站,與垃圾車沿途/定點清運的逐時刻資料語意不符、覆蓋範圍也不完整,**列為次要交叉核對來源,不作為主力資料源** | 資料源死活與授權範圍必須先於任何技術實作確認,避免在不確定合法性/可行性的狀態下投入 pipeline 開發;桃園這次比高雄更複雜的地方在於連「舊平台是否還活著」都需要先排查(DNS 層級死亡,不只是 API 退役),且真正的資料源(`route.tyoem.gov.tw`)不在 Jun 原始給的候選名單中,是排查過程中才浮現的 | 自主(資料源調查與授權確認)+ Jun 拍板(採用 route.tyoem.gov.tw 為主要資料源、PDF 降為次要來源)

2026-07-23 | 桃園資料形狀與現有系統相容性:三項欄位缺口的處理方式拍板(D1-D4)。**資料形狀**(依 `route.tyoem.gov.tw` 內部查詢端點 `POST /web/dataManagerAgentWeb.jsp`,`dcfid` 分派模式,`lagifQueryRouteByTown`/`lagifQueryTimeTableDetailByRoute` 兩個函式取得行政區路線清單與逐路線時刻表明細,需先 GET 首頁取得 `random_form` token + session cookie):13 個行政區共 315 條路線(一般路線 249、社區/市場專線 66),抽樣 7 條路線推估全市約 6,300 個清運點,與官方自述量級吻合;每個清運點記錄只有到站時間(`arrive_time`)沒有離站時間欄位,抽樣 137 筆零區間格式;沒有欄位能區分「定點清運」或「沿街收運」;`car_type=lagi`(一般垃圾)與 `recycle`(資源回收)並非一律回傳相同內容——4 條一般路線兩者逐筆相同,但社區/市場專線(如中壢魚市場、蘆竹社區專線)recycle 回傳空陣列;里別欄位(`show_memo`)在社區專線常整批為空字串,市場型路線甚至塞車牌號碼(如「KEP-3187」)取代里別;但資料源自帶穩定整數 `poi_id`,是高雄/台中資料源都沒有的。**D1(depart=null)**:`scheduleTimeText()` 新增第三分支,`depart===null` 時顯示「約 HH:MM 抵達」,不得用 arrive+N分鐘推算填補;`todaySummarySentence()` 同步處理,`ScheduleEntry.depart` 型別改為 `string | null`。**D2(point_id/id-map)**:point_id 雜湊規則(里別+清運點名稱)維持不變、不為桃園特例改規則;社區專線里別空值屬於「同 key 多筆」的既有 id-map 消歧機制正常輸入範圍,里別空值需統一正規化寫法(擇一固定表示,避免跨季雜湊結果因空值表示法不同而飄移);id-map 消歧指紋主用 `poi_id`,「車牌+首個抵達時間」降為配對失敗時的備援,不整個取代既有機制(對稱高雄/台中)。**D3(collection_type=null)**:來源無欄位可判斷定點/沿街,不斷言其中一種、也不用 `poi_name` 文字模式做啟發式猜測;`CollectionPoint.collection_type` 型別改為 `string | null`,連帶修正 `d1/schema.sql` 既有的 `collection_type TEXT NOT NULL`(桃園若寫入 NULL 會直接觸發 SQL constraint 錯誤,本機檔案已修正,但正式環境 D1 資料表既有結構未變,實際匯入桃園資料前需對正式 D1 執行 ALTER TABLE,屬影響 production 的動作,依鐵律 7 待 Jun 同意才執行);`introSentence()`/`todaySummarySentence()` 增加 `typeKnown` 分支,型別未知時一律用不預設定點/沿街的中性措辭;`[point].astro` 的「清運方式」區塊 `collection_type` 為 null 時整段不顯示。**D4(recycling_schedule)**:比照鐵律「不猜測拼接」原則,不得用「一般路線一律照抄 schedule」當全域假設(目前只驗過 4 條一般路線),pipeline 需逐路線各自查一次 `car_type=recycle`;查詢結果為空時 `recycling_schedule` 存 NULL,沿用高雄既有慣例(前端 `hasRecycling` 已會自動跳過區塊,不需改前端);有資料且與 schedule 相同時仍如實寫入(資收車同時段隨車對使用者是有用資訊),但正式資料落地後需截一頁實際頁面回報,由 Jun 決定要不要把兩張內容相同的表格改成一行註記。以上 D1-D3 已完成程式碼修改並通過 `npm run build` + `tsc --noEmit` 型別檢查,對既有高雄/台中頁面零回歸(逐一驗證輸出字串與改動前相同);D4 是 pipeline 設計原則,桃園的 `fetch.py`/`normalize.py` 尚未開始寫,待實作時依此執行 | 高雄/台中兩次資料源都是「先假設資料形狀跟既有城市一樣、事後才發現不一樣」造成後續補救,這次先把資料形狀(尤其是缺 depart、缺 collection_type 分類、recycle 不保證跟 lagi 相同)逐項與下游六個消費端(WeekdayBadge/scheduleTimeText/[point].astro/index.astro/data.ts/search.ts)對照過一輪,才開始寫桃園專屬 pipeline,避免重蹈 2026-07-22 point_id 解析失敗的同一種「只驗自己這層,沒驗完整路徑」的錯 | Jun 拍板(D1-D4 四項處理方式)+ 自主(資料形狀調查、下游相容性檢查、程式碼實作)

2026-07-24 | 桃園「一般垃圾星期缺失」(70.6% 記錄僅有「班表時間:HH:MM」、無逐日星期欄位)的處理方式與下游相容性修復拍板(F1-F2)。**F1(資料形狀與文案)**:確認 29.4% 有明確星期清單的記錄照常解析成 `weekday` 1-7;70.6% 僅班表時間的記錄,`ScheduleEntry.weekday` 一律填 **空陣列 `[]`,不用 `null`**——型別維持 `number[]` 不變、`.length === 0` 是天然的 guard、不必在每個消費端加 null 防禦;`recycling_schedule` 的星期仍依 F1 開頭確立的官方 JS 邏輯從 `run_type`(值=2 的位置)解出,不受本次影響。**語意不變量**(寫死進 `site/src/lib/data.ts` `ScheduleEntry.weekday` 註解,避免半年後被誤讀成「這些天都不收運」):空陣列只可能是「星期未知」,不可能是「這些天都不收運」,因為凡出現在 `schedule`/`recycling_schedule` 裡的 entry 必定代表這個點有收運,只是星期本身缺欄位;判斷一律呼叫具名 helper `isWeekdayUnknown(entry)`,不直接寫 `.length === 0` 比較,把語意集中在一處。頁面文案採 Jun 定稿的**版本 B**:「本站僅取得此清運點的到點時間,未取得收運星期,故不顯示星期標籤。完整班表請以{官方查詢系統}為準。」,官方查詢系統連結為 `https://route.tyoem.gov.tw/`(桃園市環境管理處「垃圾清運路線即時查詢系統」,來自 `pipeline/fetch_taoyuan_raw.py` 開頭註解),存成 `content.ts` 的 `OFFICIAL_QUERY_SYSTEMS` 對照表(目前僅桃園一筆,鍵值為 `point.city`),沒有對應城市時降級為「官方查詢系統」泛稱、不渲染連結,不得瞎猜 URL。**F2(下游相容性稽核與修復)**,先出稽核清單經 Jun 確認 `[]`/文案版本後才動手,實際修復九處:①`pipeline/validate.py` L2 檢查原本把空 `weekday` 一律判定為 `weekday_violation`(雖未實際卡 `passed` 欄位,但誤報告警數字),改為只在陣列內有超出 1-7 範圍的值才算違規;②`WeekdayBadge.astro` 空陣列時不再渲染全暗的 7 格徽章(那看起來像「這天都不收」),改為呼叫端自行決定顯示 `—` 或不顯示;③`data.ts` 新增 `isWeekdayUnknown()` helper 並在型別註解寫下不變量;④`content.ts` `todaySummarySentence()` 對「今天沒有匹配到班次」的情境新增第三種分支——原本只有「今天收運/今天沒收運」二分,現在區分「有已知星期但今天不是」vs「星期本身未知,無法判斷」,後者不得講「今天休收」這種肯定句;⑤`introSentence()`(即 `<meta description>` 來源)在 `scheduledDays.length===0` 時改用不提星期的句型,修掉原本會產生「固定於 　10:00〜10:30 收運」(星期文字被抽空後留下語意不全空格)的既有 bug;⑥`[point].astro` 主表格、資源回收表格皆在對應 entry 星期未知時顯示 `—` 並在表格上方各自顯示一次(不逐列重複)F1 文案 + 條件式官方連結;⑦`[point].astro` frontmatter 移除死碼(`const todayEntry = todayScheduleEntry(...)`,算出來從未被引用,但只要碰到 `null` weekday 會在任何 HTML 渲染前讓整頁 500——順手清掉,不留這個地雷);⑧行政區 `[district]/index.astro` 時段總表比照改 `—`,並在表格下方加一句「部分清運點本站尚未取得收運星期資料」的頁級提示;⑨JSON-LD(`seo.ts`)確認全站無 `weekday`/`dayOfWeek` 欄位,本次改動不影響、也不新增結構化資料(既有的另一個內容缺口,列為範圍外)。以上九項改完後 `npm run build` 通過,並用本機 D1(`wrangler d1 execute POINTS_DB --local`)實際把一筆既有高雄測試資料的 `weekday` 暫改成 `[]`、`city` 暫改成「桃園市」,`astro dev` 起本機伺服器逐一 curl 驗證清運點頁與行政區頁:確認① 200(無 500)、②`<meta description>`/今天答句/兩張表格皆為降級文案而非誤導性肯定句、③官方連結在有對照表的城市正確渲染、無對照表時優雅退化為純文字、④正常(星期已知)清運點頁面輸出字串與改動前逐字相同(零回歸);驗證完畢已把測試資料還原回原值,未留下任何測試髒資料 | 高雄/台中兩次事故(2026-07-20 部署、2026-07-22 point_id)都是「只驗自己這層,沒驗完整下游路徑」,這次趁 weekday 這個新資料形狀還沒真的隨桃園資料落地前,先靜態列出全部消費端(WeekdayBadge/todayScheduleEntry/todaySummarySentence/introSentence/兩處表格/data.ts 型別/JSON-LD)並實測用真資料庫驗證,而不是等桃園 normalize.py 寫完、資料推上 D1 後才發現某個頁面因為 null/undefined 壞掉,或講出「今天沒收運」這種比缺資料更糟的錯誤結論(牴觸鐵律 2 的精神:即使沒有明講填假資料,講錯誤的推論結論在使用者體感上是同一種傷害) | Jun 拍板(空陣列不用 null、F1 文案版本 B、稽核清單優先序)+ 自主(稽核清單、九項程式碼修復、本機 D1 實測驗證)

2026-07-24 | F1/F2(見同日前一條)二次拍板,修正兩處。**①驗證依據改成資料驅動,不靠猜**:`pipeline/validate.py` 原本靠「值長什麼樣子」(空/非空)推測該筆是「本就該空」還是「真的壞了」,拍板改為由 normalize.py 逐筆標記 `weekday_source: 'listed' | 'absent'`(留在 `data/normalized/*.json`,`listed`=來源本身有明確星期清單、`absent`=來源只有「班表時間:HH:MM」天生沒有逐日欄位),`check_l2_reasonableness()` 依標記分流:`absent` 卻非空才算違規(標記與結果矛盾)、`listed`(或舊城市尚未套用此標記的 `unspecified`)則維持「非空且值域合法」的原有把關;報告額外印出 `weekday_source_counts`/`weekday_source_rates`,讓「桃園 70.6% 應為 absent」這件事每季執行都能人工比對前後兩次比例是否有異常變動(來源格式改了會反映在這個比例上)。`pipeline/push_d1.py` 新增 `strip_weekday_source()`,推 D1 前把這個純稽核用欄位從 `schedule`/`recycling_schedule` 每筆 entry 拿掉,不讓它跟著進 D1 或傳到前端(此欄位目前尚未真正被任何 normalize_record_* 寫入,因為桃園專屬的 normalize.py 還沒開始寫;這次是先把「怎麼標記、怎麼稽核、怎麼在推送前濾掉」三件事的契約定下來,寫桃園 normalize.py 時依此加上標記即可,不必屆時重新設計)。**②文案拆成兩個位置,meta description 不得有免責聲明**:F1 版本 B 拿掉「故不顯示星期標籤」這種談自家 UI 的話,`content.ts` 的 `weekdayUnknownNotice()` 改為「本站僅取得此清運點的到點時間,未取得{收運/回收}星期。完整班表請以{官方查詢系統}為準。」,此文案只出現在頁面正文(`[point].astro` 班表表格上方,維持原本的位置與條件式官方連結不變);`introSentence()`(`<meta description>` 來源,5,000+ 頁規模)的 `scheduledDays.length===0` 分支改為完全不提星期、不提免責聲明的乾淨單句(如「這是三民區安吉里的其中一個清運點,垃圾車到站時間約 16:25〜16:29。」),避免全站數千頁 meta description 掛同一段重複文字,對 SEO 沒好處也浪費字數;`todaySummarySentence()`(頁面正文的「今天答句」)因為本來就不是 meta 來源,維持原本會講「請以官方系統為準」的降級文案,不受此次調整影響。以上兩處改完後 `npm run build` 通過,`.venv/bin/python` 對 `check_l2_reasonableness()` 跑合成測試(`absent`+空/`listed`+空/`listed`+合法/`listed`+超出範圍/`unspecified`+合法五種組合)驗證違規判定與計數皆符合預期,並重跑一次本機 D1 curl 驗證確認 `<meta description>` 不再包含免責聲明字樣、頁面正文的表格旁提示文字正確拿掉「不顯示星期標籤」字樣、官方連結渲染不受影響、測試資料驗證後已還原 | 稽核依據不能只看資料「長什麼樣子」去反推意圖,那樣下次資料源格式微調就會誤判(空陣列可能是預期,也可能是解析器壞了,兩者從值本身看不出差別),必須讓產生端明確標記意圖、稽核端只依標記判斷,才是真正「有可判斷依據」而非憑感覺;meta description 是全站規模最大的重複性風險(5,000+ 頁),任何要塞進去的文字都要先問「這句話幾千頁重複出現對 SEO 是加分還是扣分」,免責聲明類文字只適合放使用者真正會讀到的頁面正文,不適合放進搜尋引擎索引用的描述欄位 | Jun 拍板(weekday_source 標記方案、meta description 不得含免責聲明)+ 自主(validate.py/push_d1.py/content.ts 程式碼修改、合成測試與本機 D1 複驗)

2026-07-24 | F1/F2 第三次拍板,三處微調。**①優先序調整,第9點與第1點併作一組**:`pipeline/push_d1.py` 新增 `sanitize_schedule_entries()`(取代原本只做欄位過濾的 `strip_weekday_source()`),在推 D1 前把 `weekday` 一律轉成陣列(`None`/缺欄位/JSON `null` 一律視為 `[]`),與 `weekday_source` 過濾合併成同一道「D1 寫入端最後保證」——這正是讓 2/3/4/5(消費端各處降級邏輯)得以成立的前提,兩者都在 pipeline 階段,一起做比較順,故調整順序提前。已用合成測試驗證 `None`/缺 key/`[]`/非空四種輸入皆正確轉換,且 `weekday_source` 正確被拿掉。**②第7點死碼確認已清除**:`[point].astro` frontmatter 的 `todayEntry`/`todayWeekdayTaipei`/`todayScheduleEntry` 相關死碼與未用 import 在第一輪修復時已一併移除(grep 全檔案確認零殘留),不需要另外動作。**③第6點行政區時段總表改為單純留白**:移除原本在表格下方加的「部分清運點本站尚未取得收運星期資料」頁級提示段落(連同 `hasWeekdayUnknownPoint` 計算與 `isWeekdayUnknown` import 一併移除,`[district]/index.astro` 不再需要);星期欄維持原本已實作的 `entry.weekday.length > 0 ? <WeekdayBadge/> : '—'`,不額外加解釋文字——行政區列表頁是掃視用途,完整說明只留在清運點詳情頁(`[point].astro` 表格上方的 `weekdayUnknownNotice` + 官方連結,已於前次拍板實作,未變動)。三處改完後 `npm run build` 通過,並再次用本機 D1 暫改測試列(`weekday=[]`)curl 驗證行政區頁該列顯示 `—`、無多餘段落,其餘列徽章正常,驗證後已還原測試資料 | 把「保證資料形狀正確」和「稽核資料是否符合預期」放在同一階段一起做,邏輯上更緊密(9 是 1 的前提);列表型頁面塞長句解釋是內容雪崩的開始——5,000+ 清運點裡任何一個字級的重複文案,乘以頁面數量都是可觀的雜訊,原則上「掃視頁只留視覺提示、完整說明留給詳情頁」應作為之後其他類似降級情境的預設做法 | Jun 拍板(優先序調整、行政區頁改純留白)+ 自主(push_d1.py 程式碼修改、死碼確認、行政區頁清理、複驗)

2026-07-24 | 桃園市 normalize.py 正式實作完成(Phase 4.5 E3-1〜E3-7),資料層跑通、驗證通過,待 Jun 驗收後再進入頁面生成。**E3-1(路線內聯集)**:同一 routing_id 底下 car_type=lagi/recycle 兩次查詢結果依 poi_id 聯集,共 7,154 筆原始聯集(與 Jun 已確認數字一致),**不做任何跨路線 poi_id 合併**(全量驗證確認 229 個 poi_id 值橫跨多個 routing_id 重複出現,若當全域唯一鍵合併會誤殺);同一實體點被多路線服務者(如八德區「三鶯品苑」同時是一般路線與純資源回收路線 lagi2-002_C_5 的停靠點),交由下游「里別+清運點名稱」雜湊 + id-map 消歧處理,已驗證產生兩個獨立 point_id,未被誤合併。**E3-2(解析)**:以 show_arrive_time/show_recycle_arrive_time 為主,兩種合法格式——「班表時間:HH:MM」(weekday=[],weekday_source='absent')與「星期X,Y,Z:HH:MM」(可能多行,不同星期群組各自不同時間,每行各自一筆 weekday_source='listed');未知格式一律丟棄該行並記錄進 notes,不猜測填補。實測命中 3 筆來源瑕疵記錄(poi_id 24455 的 show_recycle_arrive_time 打字錯多一位數「19:122」、poi_id 2421 五行星期時間中一行用分號誤植「10;03」),依規則以 arrive_time 交叉檢查,前者 arrive_time 本身也帶同樣瑕疵故無法復原,兩者最終皆如實捨棄該行、如實記錄原因,**未丟整筆**(前者的一般垃圾班次、後者其餘四個星期的班次皆正常保留)。depart 一律 null,不推算填補(沿用 D1)。**recycling_schedule 星期解法**(本輪確立,經逆向核對官方 `route.tyoem.gov.tw/style2015/js/park.js` 原始碼 `showRealtimeRecycleCarStatus()`/`displayRoutingMemo()` 兩處邏輯,非猜測):`run_type` 為 7 字元字串,索引依 JS `Date.getDay()` 慣例(0=日...6=六),值為 '2' 代表當天有資源回收車;show_recycle_arrive_time 本身已列明星期時直接採用,只有它是「班表時間」單純格式(星期未知)時才用 run_type 補上,若 run_type 本身也解不出任何一天(如 lagi2-002_C_5/C_6 兩條純資源回收路線,run_type 皆為「0111111」無任何 '2')才誠實標記 absent——但實測這兩條路線的 show_recycle_arrive_time 本身就已列明星期,根本不需要落到 run_type 這個備援分支,最終全市 recycling_weekday_absent=0(每一筆資源回收班次都有確定星期來源)。**E3-3(欄位)**:里別取 show_memo,空字串統一正規化為 `None`(與高雄/台中既有慣例一致);lat/lng 直接寫入(已驗證同一 poi_id 的 lagi/recycle 兩次查詢座標零筆不一致);collection_type 一律 None(沿用 D3,來源無法區分定點/沿街);fetched_at 用各原始檔案 mtime 逐點換算(本批 630 檔皆為 2026-07-23 單日抓取,已於正式輸出確認)。行政區 13 個 gid 對照表直接取自官方首頁 `<select>` 下拉選單原文(蘆竹/八德/桃園/中壢/平鎮/楊梅/大溪/大園/觀音/新屋/龜山/龍潭/復興),非音譯猜測。**E3-4(id-map)**:雜湊 key 維持「里別+清運點名稱」不變;`pipeline/point_id.py` 的 `assign_point_ids()` 消歧欄位新增 `route_scope`(僅桃園設為 routing_id,高雄/台中維持 None 不影響既有行為,已用 taichung 全量重跑逐位元組 diff 確認 `data/normalized/taichung.json` 輸出零差異、`data/id-map/taichung.json` 僅多出恆為 null 的 `route_scope` 欄位);`plate` 欄位沿用既有鍵名(不改名以維持與既有 `data/id-map/{kaohsiung,taichung}.json` 存檔格式相容),桃園存入複合字串 `"{routing_id}:{poi_id}"` 而非裸 `poi_id`(裸值已證實跨路線重複、不安全);第二輪「首個抵達時間最接近」與第二輪半「依序 position-pair」備援皆加上 `route_scope` 相等比對,避免跨路線誤配對。**桃園跨季穩定性尚未驗證**(僅這一批資料、無第二次抓取可比對),`route_scope`+複合 fingerprint 是否真能在下一季資料變動後維持 URL 穩定屬理論設計,未被跨季真實資料驗證過;既有「未配對到的舊條目保留不刪」原則對桃園格外重要,是目前唯一的事後補救機制。**E3-5(驗證)**:過程中發現並修好兩個 validate.py 既有臥底 bug(高雄/台中至今未觸發,因為兩者從未真正用到相關欄位的 null 值)——①`recycling_schedule` 明確存 `null`(D4 既定慣例)時 `r.get("recycling_schedule", [])` 不會回退預設值,與 list 相加直接 TypeError,改用 `or []`;②`depart` 明確存 `null`(D1 既定慣例)時被 `_is_valid_time()` 判定違規,导致桃園每一筆時刻表記錄都固定貢獻 1 筆假的 time_violation(桃園全量測得 13,553 筆、剛好等於全部 entry 數,即 100% 假陽性),改為只在 `depart is not None` 時才檢查其格式。兩處修復皆已用 kaohsiung/taichung 全量重跑確認 violations 數與修復前相同(零回歸)。修復後桃園 L1/L2/L3 全數通過(L1 完整率 98.62%、L2 time_violations=0、weekday_violations=0、coord_violation_rate=0、L3 首次執行略過)。**回歸檢查**:lagi2-002_C_5(66 筆)、lagi2-002_C_6(32 筆)兩條純資源回收路線的清運點,以及復興區 10 條路線的 102 個清運點,皆確認出現在最終 `data/normalized/taoyuan.json`(共 7,115 筆)中,無被 E3-1 聯集或欄位過濾邏輯誤刪。**E3-6(統計)**:總聯集 7,154 筆,丟棄 39 筆(schedule 與 recycling_schedule 皆空)、98 筆為純資源回收點(schedule=[] 但 recycling_schedule 有值,對應上述兩條純回收路線)、最終 7,115 筆;一般垃圾班次 weekday 留空(absent)3,863 筆、明列(listed)4,764 筆——這個全量比例(約 45% listed)與 F1 最初提到的抽樣估計「29.4%/70.6%」不同,推測係抽樣與全量的差異(D-series決策記錄本身也提到桃園量級是「抽樣 7 條路線推估」而來),非本次解析邏輯有誤,已就地記錄不隱藏;recycling_schedule 有值比例 4,911/7,115 = 69.0%;被 ≥2 路線服務的 poi_id 有 229 個;id-map 消歧(同 base_key 對應 >1 個 point_id)共 283 組;show_*/arrive_time 不一致筆數為 0(3 筆瑕疵記錄皆為兩邊都壞、非不一致)。**E3-7**:`.gitignore` 確認 `data/normalized/`、`data/id-map/` 皆未被排除(`data/raw/taoyuan/` 已有既存例外);`npm run build` 通過;`data/normalized/taoyuan.json`、`data/id-map/taoyuan.json`、`data/meta.json`、`pipeline/{normalize,point_id,push_d1,validate}.py`、本記錄一併 commit | 本次是繼「D1 depart=null」「point_id 內容雜湊」兩次教訓後,第三次證實「改動只驗自己這層、沒驗完整下游路徑」的同一種錯誤模式——這次改在 validate.py 這一層,若不是逐一產出真實資料並實際跑一次驗證,不會發現 depart=null 和 recycling_schedule=null 這兩個既定資料形狀早就埋著會讓驗證直接 crash 或产生 100% 假陽性的隱性 bug;run_type 的星期解碼堅持「找到官方原始碼核對」而非憑資料樣式猜測,是因為猜錯的代價是對真實使用者顯示錯誤的收運星期,直接牴觸鐵律 2 的精神——寧可多花時間逆向 JS,也不用「看起來像」的規律硬套 | Jun 拍板(E3-1〜E3-7 全部處理原則)+ 自主(run_type 官方邏輯逆向核對、point_id.py 相容性改動與全量回歸驗證、validate.py 兩處既有 bug 發現與修復、正式資料產出與驗證、回歸檢查)

2026-07-24 | Gate F 上線前二次覆核,發現並修正兩個實質問題(G1、G2),重新產出桃園正規化資料(7,115→7,152 筆)。**G1(拿掉 run_type 點層級 fallback)**:E3 當時讓 recycling_schedule 的星期在 show_recycle_arrive_time 本身沒有明講時,退而用 `_decode_taoyuan_recycle_weekday()`(run_type 值=2 的位置,官方 park.js 邏輯)幫該點補值,這次拍板推翻——run_type 是「這條路線」的官方回收日定義,不保證路線底下每個點實際被服務的日子跟路線整體一致(尤其社區/市場專線,或路線內部再分次序造訪的情況),用路線層級事實斷言特定點的星期屬於跨層級猜測,即使依據是官方 JS 邏輯也不例外——鐵律 2 的精神是「不猜測填補」,不是「有官方依據就能套用」。改為:資源回收星期只能來自該點自己的 show_recycle_arrive_time,沒有明講星期時一律視為未知(`[]`,`weekday_source='absent'`),套用與收運日相同的 F1 免責文案。`_decode_taoyuan_recycle_weekday()` 函式與其官方 JS 逆向邏輯**保留**在 `pipeline/normalize.py` 與本檔案中(它仍是「這條路線」的官方回收日定義,未來若做路線層級而非點層級的內容可以直接用),但 `normalize_taoyuan()` 已不再呼叫;函式 docstring 已補上不得下放到點層級的原因說明。驗收:掃描 `data/normalized/taoyuan.json` 確認「收運日(schedule)有任一筆未知 且 回收日(recycling_schedule)有任一筆明確星期」的組合為 0 筆(修正前這個組合大量存在,即 E3 當時 `recycling_weekday_from_run_type` 統計的 3,784 筆)。**G2(查證「收運日只有一天」是否為解析器遺漏)**:桃園 176 個「單一星期、單一時段」的清運點中,抽查 3 個(含点名本身帶星期提示的「國際路一段975巷130號橋下(星期一,四,六)」)做原始 JSON 全欄位攤開(`show_arrive_time`/`show_memo_time`/`arrive_time`/`memo` 分號各段)+ 本站解析結果 + 官方顯示邏輯三方比對,結果**不是**單純的偏遠點少班次,而是解析器遺漏了 `show_memo_time`(一般垃圾)/`show_recycle_memo_time`(資源回收)這兩個次要欄位——直接核對官方 `route.tyoem.gov.tw/style2015/js/park.js` 的清運點列表與地圖資訊視窗渲染邏輯(`row.show_arrive_time...+"<br>"+row.show_memo_time`,資源回收同理),確認官方網站永遠把主欄位與 memo_time 欄位疊在一起顯示,代表這是同一個點的「兩段」班次(通常是不同星期 group、不同時間),不是可以只看其中一個的次要欄位。全市掃描:706 筆一般垃圾、699 筆資源回收的 show_memo_time/show_recycle_memo_time 非空且與主欄位內容不同(零筆重複),此前這兩段時間完全沒被解析、系統性遺漏;其中 37 筆點的主欄位(show_arrive_time/show_recycle_arrive_time)甚至整個是空字串,唯一的班次資料就在 memo_time 欄位裡,先前被誤判為「無有效班次」整筆丟棄(`dropped_empty_both` 39→2)。**修法**:新增 `_parse_taoyuan_combined_time_fields()`,把主欄位與 memo_time 欄位各自解析後合併成同一份 entries 清單;memo_time 沒有對應的裸 `arrive_time` 可交叉檢查(那個欄位是配對主欄位用的),解析失敗就直接依 E3-2 規則捨棄、不猜測複用。修正後重跑:總筆數 7,152(較修正前 7,115 增加 37,對應救回的空欄位點),L1/L2/L3 全數通過(L1 完整率 98.63%、L3 變動率 0.52% 遠低於 30% 門檻),復興區 102 點、純資源回收路線 lagi2-002_C_5/C_6 共 98 點的回歸檢查依然成立。剩餘 2 筆真正無班次資料的點(如「下內定土地公」show_memo 直接寫「有通知才清運」,屬於通知制清運、非固定班表,不是解析遺漏)如實維持丟棄。以上兩處修正後 `d1/import/taoyuan.sql` 已重新產生(publishable 7,017→7,054),**但 Gate F 當時已匯入本機/遠端 D1、且已上傳的 workers.dev preview 版本(`bb09c804-trash-pseo.junsu578.workers.dev`)都還是修正前的舊資料**,尚未重新匯入/重新上傳,下一步待重跑本機+遠端 D1 匯入與重新驗證 preview 後才能真正視為 Gate F 通過 | 兩處都是「資料形狀第一次見到的縣市,只驗過自己當時想到的情境,沒有窮舉所有欄位/所有已知型態」的同一種模式(呼應 E3 當時已經記錄的教訓)——run_type fallback 當時是為了不要讓資源回收星期空著太多筆,但「有更確定的資料來源」不等於「這筆資料的正確度」,跨層級推論本身就是風險;memo_time 欄位如果不是 Jun 主動要求做「單一星期點」的三方查證,不會被系統性抓出來,這類「次要顯示欄位其實是正文一部分」的陷阱,提醒未來遇到新資料源時,即使某個欄位看起來像是次要/補充資訊,也要先核對官方前端怎麼渲染它,不能單憑欄位命名(memo/note/備註)就假設它不重要 | Jun 拍板(G1 推翻 run_type 點層級套用、G2 查證方向)+ 自主(G1 程式碼修改、G2 三方比對調查與官方 JS 渲染邏輯核對、程式碼修復與全量重跑驗證)

2026-07-27 | 桃園上線前內容審查(H1-I9),連續兩次發現「管線驗證格式、驗證不了內容合理性」的同類漏洞後,系統性補起來,桃園正式在 `mengwaba.com` 上線。**起因**:Jun 兩次上線前人工抽看各發現一個文案 bug(先是回收日 route-level fallback 誤用,後是 `introSentence()` 對 weekday 未知點外層又包一層「到站時間約」,與 `scheduleTimeText()` 已回傳的完整片語疊字重複,如「到站時間約 約 17:00 抵達」)——status code 與 L1/L2/L3 全數放行,問題只有人眼逐句讀過才看得出來,顯示管線驗證的是「資料格式合不合法」,不是「渲染出來的句子合不合理」。**H1**:上述疊字 bug 已修(`content.ts` `introSentence()` 不再重用 `scheduleTimeText()` 的完整片語外層加字,依經過/抵達/區間三種情境各自組句)。**H2 掃描**:比照同一種「呼叫端在被呼叫函式已回傳完整片語外再加前綴/後綴」的模式,檢查全部呼叫 `scheduleTimeText()`/`todaySummarySentence()`/`introSentence()` 的地方,確認只有 H1 那一處中鏢,沒有第二個同類案例。**H3 組合矩陣法**:建立「星期已知/未知 × depart 有值/null × 有無 recycling_schedule」等六維矩陣,每種組合取一筆真實資料,把三個函式的實際輸出印出來人工逐句讀——這個方法論本身在執行過程中又多抓出兩個更大規模的事實錯誤(Bug A、Bug B),證實「窮舉真實資料形狀 + 人工讀輸出」比單獨盯著已知 bug 修更有效。**Bug A(`introSentence()` 星期聯集配單一時間)**:多個 entry 且時間不同的清運點,`daysText` 是所有 entry 星期的聯集,卻只引用 `schedule[0]` 的時間,對聯集裡其他星期的時間陳述是事實錯誤(實例 `TYN-LUZHU-33C4F756E7`:週一/四實際到站時間是 17:30,舊版句子卻統一講 17:25),且這句話會直接進 meta description 被 Google 索引。規模:高雄 1,078、台中 3,664、桃園 1,470 筆。採用方向(a):偵測到多個不同時間形狀時放棄承諾單一時間,改成「垃圾車在[星期]收運,不同星期的到站時間略有不同,詳見下方時刻表」——星期本身的聯集沒有問題(保留,對 SEO 有用),放棄的只是「單一時間」這個過度精簡的承諾。`todaySummarySentence()` 確認結構上不會有同類問題,因為它透過 `todayScheduleEntry()` 依「今天」實際 weekday 去 `.find()` 對應的那一筆 entry,不是取聯集後隨便挑一筆。**Bug B(`isPassThrough()` 自行發明的 arrive===depart 判準)**:原始邏輯要求 `collectionType==='沿街收運' && entry.arrive===entry.depart` 才算沿街收運/pass-through,這個「到站即離站」的額外判準是本站自行發明、非官方資料語意——實測台中 95%(18,051/19,024)標示為沿街收運的點,實際記錄的是 5–15 分鐘的到站/離站時間窗口(arrive≠depart),導致這些點被誤判成「定點清運站/停靠」措辭,與同一頁「清運方式」欄位(沿街收運)自相矛盾。高雄同款誤判約 90 筆。修法:`isPassThrough()` 拿掉 `arrive===depart` 條件,只看 `collection_type` 這個官方標示的事實;新增 `passThroughTimePhrase()`,單一時刻用「約 X」、時間窗口用「約 X〜Y」,`scheduleTimeText`/`todaySummarySentence`/`introSentence` 三處共用同一套措辭規則。**I1(純資源回收點被自己的過濾邏輯排除,自相矛盾)**:`pipeline/push_d1.py` 的 `classify()` 原本只要求 `schedule` 非空才發佈,連帶排除了 98(桃園)+13(台中)筆「schedule 為空但 recycling_schedule 有真實資料」的純資源回收點(如桃園 lagi2-002_C_5/C_6 兩條純回收路線)——這批點正是 E3 當時特地做 car_type 聯集才救回的,結果在推 D1 這一步被同一種「非空 schedule 才算數」的邏輯抵消,聯集等於白做。改為只有 `schedule` 與 `recycling_schedule` 皆空才排除,同步修正 `site/src/lib/data-static.ts` 的 `loadCityPoints()` 保持一致(否則會出現「D1 有頁面但搜尋索引/縣市計數找不到」的不一致)。頁面層(`content.ts`)新增「僅提供資源回收收運」專屬措辭,不得誤講「今天沒有收運」;`[point].astro` 對 `schedule` 為空時不再渲染空的「本週時刻表」表格。`schedule` 是 D1 schema 裡的 `NOT NULL` 欄位,新增 `sql_json_array()` 避免空陣列被序列化成 SQL NULL 撞欄位限制。**Snapshot 測試**:新增 `vitest` + `site/src/lib/content.test.ts`,35 個 golden test,涵蓋組合矩陣全部情境(H3 六維矩陣 + I1/I2/I3 新增的單一/多時段、沿街收運兩種形狀、純資源回收點),字串皆為 2026-07-27 Jun 人工逐句讀過確認正確後才寫進測試檔;檔案頂端註明「修改前必須把新輸出重新貼出來給 Jun 人工審閱過,才能更新期望值」,目的是把「管線只驗格式、驗不了內容合理性」這個缺口用自動化補起來——之後同類文案/邏輯改動,測試會在 CI 層級直接紅掉,不必再等 Jun 上線後人工抽看才發現。**部署**:遠端 D1 備份(`d1/backups/pre-i6-taoyuan-taichung-reimport-2026-07-27.sql`)後,只重建桃園與台中兩個 city_slug(高雄未動),`wrangler versions upload` 產生隔離預覽、Jun 人工抽看後 `wrangler versions deploy` 切到正式(取代舊錨點 `9136a101-9e77-499a-ad8e-52eb99b87ea9`),部署後首頁/三城市索引/桃園多種清運點頁/高雄台中抽查頁在 `mengwaba.com` 與 `workers.dev` 兩邊皆驗證通過(部署後約 1 分鐘內兩網域曾對桃園頁面偶發 404,查證是 Cloudflare edge 對部署前「桃園尚未上線」狀態的 404 短暫負向快取,非新版本問題,清除後穩定)——桃園自此是第一次真正對外上線 | 這兩次連續事故(回收日 fallback、疊字 bug)都指向同一個結構性缺口:pipeline 的 L1/L2/L3 與 `npm run build` 只保證「資料格式合法」「程式碼能編譯」,完全不保證「渲染出來的中文句子讀起來通順、陳述的事實正確」——這種缺口只能靠(a)人在上線前實際看過渲染後的頁面,(b)組合矩陣式窮舉所有已知資料形狀並人工讀過輸出,兩者缺一不可;`isPassThrough()` 的 `arrive===depart` 判準是「看起來像規律就拿來當判準」的典型錯誤(呼應 G1 對 `run_type` 猜測的同一種教訓),collection_type 這種官方明講的事實欄位不該再疊加自行發明的次要判準去覆蓋它的語意;`run_type` 為路線層級語意,不可下放至點層級套用到個別清運點(G1 已拍板,本次未推翻,仍然有效)| Jun 拍板(H3 組合矩陣方法論、I1/I2/I3 修法方向、部署節奏)+ 自主(H1/H2 疊字 bug 定位與修復、Bug A/Bug B 發現與規模量測、I1-I3 程式碼實作、snapshot 測試建置、部署與驗證)

2026-07-28 | S1-S4 動態依城市拆分 sitemap 正式部署上線(取代舊的 5-URL 靜態 sitemap):push 分支後 `wrangler versions upload` 產生版本專屬預覽(`07188961-0d5e-4476-8e4d-53489a6c33fd`),對預覽網址做紅線驗證(`sitemap-index.xml`/三城市 sitemap 皆 200,網址數與 D1 筆數一致:高雄 19,472、台中 20,016、桃園 7,166;抽樣清運點頁皆 200),取得 Jun 同意後記下舊錨點(`b0dcf3aa-c6c0-4c5d-ae1e-02e6849c4374`,2026-07-27 桃園 H1-I9 版)才 `wrangler versions deploy` 切正式。部署後約 1 分鐘內 `mengwaba.com` 與 `trash-pseo.junsu578.workers.dev` 對部分城市 sitemap 偶發 404(與 2026-07-27 桃園上線時同款 Cloudflare edge 對「部署前尚未存在」狀態的短暫負向快取,非新版本問題),3 次重試後穩定為全部 200。上線後複驗:三城市 sitemap 網址數與 D1 一致、首頁/`/trash/`/三城市索引頁/行政區頁/清運點頁(含桃園純資源回收點)/搜尋索引/robots.txt(已指向新 `sitemap-index.xml`)皆 200 且無回歸,抽樣清運點頁 JSON-LD(BreadcrumbList)與 title 正常。GSC 尚未提交,待下次確認 | 鐵律 8/9 要求的「版本專屬預覽 → 紅線驗證 → 明確同意 → 正式部署 → 上線後複驗」完整流程,這次從 push 到部署到複驗一次走完,沒有跳步 | Jun 拍板(部署同意)+ 自主(push、預覽驗證、部署執行、上線後複驗)

2026-07-28 | 新北市 Phase 4 資料層擴充(A1-A4:fetch→normalize→validate→commit,不含 D1 推送與 Astro 頁面,僅資料層):city_key 定為 `xinbei`(拼音,對齊 `kaohsiung`/`taichung`/`taoyuan` 既有命名慣例),point_id 前綴 `XBC`。實際打 API(`data.ntpc.gov.tw/api/datasets/edc3ad26-8ae7-4916-a00b-bc6048d19bf8/json?size=30000`)取回 26,671 筆(與平台標示筆數吻合),原始回應完整落地 `data/raw/xinbei.json`(2026-07-21 高雄原始資料未進版控遺失、需重跑的教訓,這次先開 `.gitignore` 例外 `!data/raw/xinbei.json` 再抓取,並用 `git add -n` 實際驗證可追蹤,不是憑感覺)。29 個行政區對照表(`XINBEI_DISTRICT_MAP`)逐一比對實際資料 `city`(來源這個欄位語意其實是行政區,不是縣市層級,寫程式時特別用 `district_raw` 避免與本站 schema 的 `city` 混淆)欄位的 29 個不同值,程式跑完 `district none count: 0`,確認 29 區全數對上、無遺漏。星期旗標為 `garbage`/`recycling`/`foodscraps` 三組各七個英文星期全名後綴欄位(非高雄/台中慣用的 d1..d7),全量掃描 26,671 筆 187,697 個旗標only 出現 `"Y"`與空字串兩種值,0 筆未知格式。時間僅單一 `time` 欄位、無離站時間,`depart` 一律 `None`(比照桃園慣例,不推算);三種類型共用同一個已解析的 time 值,不重複解析。`collection_type` 留 `None`(比照桃園 D3,來源無此欄位、memo 裡零星「(持續X分鐘)」等自由文字不得反推)。`memo` 非空(4,213/26,671,69 種相異值,多數為行政管理性質如「國定假日、連假隔天會加收」2,553 筆)原樣併入 `notes` 顯示,不做任何結構化推斷。廚餘(`foodscraps_schedule`)是本次新增的 schema 維度,現有三縣市都沒有;為此新增 `d1/migrations/002-add-foodscraps-schedule.sql`(比照 001 的建新表模式,只建檔案不執行,待 Jun 核准)並在 `d1/schema.sql` 本體加上欄位定義與「既有正式表需靠 migration 套用」的註解。`point_id` 消歧:60.1%(9,078/15,094 組 `village+name`)的清運點橫跨多個 `lineid`(常見為下午班+晚上班兩趟造訪同一點,已逐一核對座標 100% 一致,非誤判),比照台中「同址不同班次視為不同頁面」慣例,每個 `lineid+rank` 組合各自成為獨立清運點、不做跨路線 union;`assign_point_ids()` 呼叫時 `plate` 傳 `None`(新北無車牌欄位)、`route_scope` 傳 `lineid`(鎖定同一路線範圍內做「最接近抵達時間」配對,避免跨路線誤配對,道理同桃園用 `routing_id` 當 `route_scope`)。為了讓 `route_scope` 能傳遞,`normalize_city()` 通用驅動函式的 `identity_rows` 新增 `"route_scope": r.get("_route_scope")`(用 `.get()` 取值,高雄/台中沒有 `_route_scope` 暫存欄位時安全回退 `None`,行為不變)。實測驗證:①同一批原始資料重跑兩次,26,671 個 point_id 逐一比對完全相同;②把原始資料列順序打亂後重跑,以 `(lineid, rank)` 為基準比對,26,671 筆對應的 point_id 零筆不一致——確認消歧邏輯不依賴來源回傳順序,跨季更新 URL 能維持穩定。`validate.py` 的 `check_l2_reasonableness()` 新增掃描 `foodscraps_schedule`(原本只掃 `schedule`+`recycling_schedule`),這是共用程式碼,已用「改動前後跑一次高雄/台中/桃園、diff 兩次報告」的方式回歸測試,除了 `validated_at` 時間戳以外**逐位元組完全相同**,確認零回歸。新北 L1/L2/L3 全過:總筆數 26,671,L1 完整度 99.99%(2 筆 `schedule` 為空,僅有回收/廚餘資料,不算異常,是純資源回收/廚餘點);一般垃圾 `schedule` 有值 26,669 筆(99.99%)、資源回收 24,502 筆(91.87%)、廚餘 26,564 筆(99.60%);經緯度 100% 覆蓋、零筆越界;29 區分布逐一核對與資料可行性勘查報告一致(板橋 6,779、新莊 4,706、土城 2,357、永和 2,198、新店 2,002、中和 1,567、樹林 1,082、三重 934、淡水 643、五股 642、泰山 454、蘆洲 440、汐止 357、林口 342、三峽 272、瑞芳 264、金山 235、萬里 215、鶯歌 179、深坑 162、八里 135、石碇 128、三芝 110、雙溪 100、坪林 100、貢寮 99、烏來 91、石門 62、平溪 16)。`site/` 目錄 `npm run build` 通過、既有 35 個 snapshot 測試全過(不受資料層改動影響,純回歸確認)。本輪**明確不含**:未註冊進 `site/src/lib/data.ts` 的 `CITIES`、未執行 `pipeline/push_d1.py`、未跑任何 `wrangler d1 execute`(含 --local)、未 git push、未部署——這些留待 Phase B 與 Jun 後續拍板 | 資料真實性與消歧穩定性優先於單純把資料塞進 schema:新北的 60.1% 多路線重複點如果誤判成同一物理點做 union,會讓「下午班」「晚上班」兩種不同時段的服務混成一筆錯誤的班表;`lineid`/`rank` 只拿來當 `route_scope`/消歧輔助信號、不當全域唯一鍵或雜湊 key 的一部分,是桃園 `poi_id` 教訓的直接延續;`validate.py` 是所有縣市共用的把關程式碼,任何擴充都必須用實際 diff 證明對既有縣市零副作用,不能只憑「理論上安全」就跳過回歸測試 | 自主(程式實作、實測驗證、A1-A4 全流程)+ 待 Jun 拍板(D1 migration 002 實際執行、collection_type/廚餘後續頁面呈現方式、Phase B 是否啟動)

2026-07-28 | **point_id 雜湊化 SEO 事故與修復**:2026-07-22 point_id 從全域流水號改內容雜湊(見同日稍早條目)後,已被 Google 索引的舊格式清運點網址(如 `/trash/kaohsiung/meinong/06852/`)全數 404,GSC 顯示已索引 2,753 頁,7/19–7/21 尚有曝光 1,440、點擊 24,7/22 起雙雙歸零。**根因**:改 ID 方案當時所有檢查都放在「新資料能不能正確顯示」,沒有人問「已經被索引的舊網址怎麼辦」——與 2026-07-22 稍早 `parsePointId` 不相容那次是同一類盲區(只驗證內部消費端,沒考慮外部既有引用),已寫入 CLAUDE.md 鐵律 10。**修復流程**:①用「里別+清運點名稱」當連結鍵,比對 git 歷史最後一版舊格式資料(高雄 commit 24f32cc6/2026-07-09、臺中 commit b713a4f6/2026-07-16)與現行雜湊資料,同址多筆用 schedule/recycling_schedule 內容特徵消歧,無法唯一判定的一律放棄、不猜測配對——臺中舊資料本身沒變(只是重新雜湊),100%(20,004/20,004)可信賴對照;高雄同時換了資料源(CSV→PDF,見 2026-07-21 條目),41.4%(7,884/19,038,含 7,254 筆完全查無 + 630 筆多筆候選無法唯一判定)在新資料裡查無對應,如實記錄維持 404。②新增 D1 表 `legacy_point_redirects`(全新增量表,`d1/migrations/003-legacy-point-redirects.sql`,不動既有 `points` 表,不需要 001/002 那種建新表搬資料的重建流程),`pipeline/build_legacy_redirects.py`/`pipeline/push_legacy_redirects_d1.py` 產生對照表與匯入 SQL。③`[point].astro` 新增:純數字 pointSlug(舊格式明確特徵,新雜湊格式必含字母、兩者不重疊)查這張表,查到就 301 導向(用新 point_id 解析出的行政區組網址,避免行政區本身也變動時導向到錯誤網址)、查不到維持誠實 404,不猜測配對到其他點。**驗證**:本機 `--local` D1 抽樣高雄/台中各 40 筆有對照的舊網址 80/80 全部 301→200,30 筆查無對應/多筆無法唯一判定的舊網址 30/30 正確維持 404;`npm run build`、35 個 snapshot 測試皆過;`validate.py`/`data-d1.ts` 共用程式碼零回歸。遠端 D1 執行前先備份(`d1/backups/pre-legacy-redirects-20260728_1526.sql`,46,573 筆),migration 003 確認為裸語句(無 BEGIN/COMMIT)且只建新表不碰 `points`,執行後 `legacy_point_redirects` 高雄 11,154、臺中 20,004 與本機一致,`points` 表三市筆數(19,435/19,986/7,152)寫入前後完全相同。`wrangler versions upload` 版本專屬預覽(`f3c56863-1c7a-4f8e-9b09-5472042227a4`)上用同一套 80+30 筆抽樣重跑,結果與本機一致,另確認首頁/三城市索引頁/行政區頁/桃園清運點頁/動態 sitemap 皆 200 零回歸。**GSC 曝光交集分析**(匯出視窗 2026/7/8–7/22,`data/gsc-export/網頁.csv`):950 筆舊格式清運點網址有曝光紀錄,總曝光 2,699、點擊 37,**100% 是高雄**(臺中 0%,非統計異常——臺中舊格式網址 7/16 上線、7/23 換 ID,曝光視窗內只有約一週,還沒累積到自然曝光就換了格式,不代表臺中完全沒受影響)。可導向(mapped)佔 1,964 曝光(**72.8%**)、27 點擊;查無對應(unmapped)佔 735 曝光(**27.2%**)、10 點擊,分散在 269 筆網址,無單一頁面集中(最高單筆 16 次曝光),意即這次 ID 換法造成的**確定性損失量化為:約 27% 既有曝光(735 次)、269 個頁面,無法透過本次修復挽回**。曝光層級覆蓋率(72.8%)高於整體記錄層級覆蓋率(58.6%)的原因:推測是真正有自然曝光的熱門點,地址/里別資訊較完整規範,新舊資料間更容易唯一配對成功 | 已被搜尋引擎索引的網址是資產,不是可以任意汰換的內部識別碼,任何 ID/URL 結構變更都必須同時提出舊網址處理方案並上線前驗證(鐵律 10);查無對應的部分寧可誠實 404、也不猜測配對到錯誤的新網址,避免比誠實 404 更差的軟性 404 或錯誤導向;GSC 曝光量化分析證明這次修復值得做(挽回近 73% 曝光價值),也留下「當初換 ID 代價」的確切數字供未來參考 | Jun 拍板(修復優先順序高於新北開發、每一步部署前的明確同意、GSC 匯出提供)+ 自主(考古比對、消歧邏輯、程式實作、本機與遠端驗證、GSC 交集分析)

**部署後補記(2026-07-28)**:遠端 D1 先備份(`d1/backups/pre-legacy-redirects-20260728_1526.sql`)、確認 migration 003 為裸語句且只建新表後,依序執行 `--remote` migration + 對照表匯入,`legacy_point_redirects` 高雄 11,154/臺中 20,004 與本機一致,`points` 表三市筆數寫入前後不變。`wrangler versions upload` 產生版本專屬預覽(`54adda21-6906-41a8-bf61-457127588ef6`),用 GSC 曝光清單(而非隨機樣本)抽樣驗證:曝光最高的 30 筆可導向網址 + 20 筆隨機補充共 50 筆全部 301→200,曝光最高的 10 筆查無對應網址 + 10 筆隨機補充共 20 筆全部維持 404。取得 Jun 同意後 `wrangler versions deploy` 切正式(回滾錨點 `07188961-0d5e-4476-8e4d-53489a6c33fd`)。部署後約數秒內正式網域對少數網址(`zuoying/01875`、`nanzi/07010`、`daliao/05454`)出現短暫 404/導向後 404 的不一致結果,重試 3 次後全部穩定為 301→200,與 2026-07-27 桃園上線時同款 Cloudflare edge 短暫負向快取現象一致,非新版本問題。正式網域最終複驗:GSC 曝光樣本 50+20 筆全數通過、`meinong/06852` 維持 404、首頁/三城市索引頁/動態 sitemap 皆 200,無需回滾。

2026-08-25 | **D1 免費額度超額事故與修復(第一批,清運點頁拆整區查詢)**:Cloudflare 通知 D1 免費方案將於 2026-09-01 起強制執行每日上限(500 萬 rows 讀取/10 萬 rows 寫入),本帳號經常超過。診斷(`wrangler d1 insights`)確認過去 24h 生產流量約 1,521 萬 rows read(約額度 304%),根因非缺索引(六類查詢的 `EXPLAIN QUERY PLAN` 皆走 `SEARCH ... USING INDEX`,無任何 `SCAN`),而是清運點頁(`[point].astro`)借用行政區頁的「整區」查詢(`loadDistrictFromD1`,原因是「鄰近清運點」天生要同區其他點)取單一清運點,單一請求就讀進整個行政區——板橋(6,779 點)、新莊(4,706 點)等大行政區單次請求 rows_read 可達數千,此查詢貢獻 24h 總讀取量約 87%(13,223,111 rows、9,997 次執行,平均每次 1,321 列)。次要貢獻者(約 11%)是行政區頁「其他行政區」連結用的 `SELECT DISTINCT district, district_slug ... WHERE city_slug=?`,`district` 顯示名稱不在既有 `idx_points_district(city_slug, district_slug)` 索引內,DISTINCT 需對整城市列做 table lookup(平均每次讀約 2 萬列,只回傳約 30 個行政區名稱)。**修法(第一批,本次上線)**:`data-d1.ts` 新增 `loadPointById`(依 PK `point_id` 直接查單點,rows_read≈1)、`buildPointId`(依 `CITY_POINT_ID_PREFIX` 對照表 + URL 的 city/district slug + pointSlug 組回完整 point_id,前綴對照需與 `pipeline/normalize.py` 的 `ID_PREFIXES`/`parse_kaohsiung_pdf.py` 呼叫處手動同步)、`loadDistrictDisplayName`(404 頁仍要行政區 context 時用,`LIMIT 1` 不撈整區)、`loadNearbyCandidates`(鄰近清運點候選集,見下方取捨說明)。`[point].astro` 全面改用上述函式,不再呼叫 `loadDistrictFromD1`;行政區頁(`[district]/index.astro`)本來就要顯示全區、且只有約 130 個網址,維持原查詢不變。**測試中發現的三個技術陷阱(逐一記錄避免重蹈)**:①**SQLite 的 LIMIT 陷阱**——`loadNearbyCandidates` 最初設計為「同村里優先查詢 + LIMIT 200」,理論上該只讀同村里的列,但實測發現殘餘條件(`village=?` 不在任何索引內)搭配 LIMIT 時,若 LIMIT 大於實際符合筆數,SQLite 無法提早停手,必須掃到索引鎖定範圍(此例為整個行政區)結尾才能確定「沒有更多符合的了」。實測:板橋玉光里(實際 30 筆)+ LIMIT 200,`rows_read`=6,780(整個板橋區);同一查詢改 LIMIT 10(低於實際 30 筆),`rows_read`=61(正常)。縮小 LIMIT 不是解法(事先不知道每個村里實際筆數,村里若剛好只有 5 筆,LIMIT 10 一樣會掃到整區結束),正解是幫該欄位建索引。②**D1 的 rows_read 計的是掃過的索引項目數,不是回表次數**——為解決上述①與行政區頁 DISTINCT 查詢的效率問題,新增 `d1/migrations/004-nearby-and-district-covering-indexes.sql`(`idx_points_village (city_slug, district_slug, village)`、`idx_points_district_display (city_slug, district_slug, district)` 兩個純新增索引,不動資料),`idx_points_village` 完全達標(板橋玉光里同一查詢 rows_read 從 6,780 降到 31);但 `idx_points_district_display` 對 DISTINCT 查詢**沒有效果**——`EXPLAIN QUERY PLAN` 顯示 `SEARCH ... USING COVERING INDEX`,但實測 `rows_read` 維持 26,671(整個新北)不變,因為 DISTINCT 這個操作本身仍需要逐一檢視該城市範圍內每一個索引項目才能判斷是否重複,covering index 只省去「不用再回主表查」這一步,不等於「不用逐列檢視」,SQLite 對這種 pattern 沒有 index skip scan 優化。這項優化**未納入本次上線**,列入下一批(改用 build-time 靜態 bundle 或依 sitemap 模式做邊緣快取,取代 D1 查詢)。③**已知取捨(非 bug,Jun 拍板接受)**:`loadNearbyCandidates` 對有村里資料的點,優先查同村里(候選集上限 200 筆,同村里筆數 <15 才擴大到整個行政區,候選集上限 300 筆);對村里為 `null` 的點(桃園部分社區專線約 31% 無村里資料)直接走擴大查詢。有效座標(新北全量、桃園部分)的排序仍是既有 `nearestPoints()` 的 haversine 精算,只是精算範圍限縮在候選集內,不再是整個行政區——意味著極少數情況下,真正地理最近的點若剛好落在候選集外(尤其村里為 null、需要擴大到整區抽樣的情況),不會被選中。頁面用語是「鄰近清運點」而非「最近的五個」,語意上成立,是刻意的取捨。**驗證**(無自動化測試涵蓋 `nearestPoints()`/鄰近清運點渲染,74 個既有測試只涵蓋 `groupByVillage`/`computeDistrictStats`/`fetchedAtDateRange`/`scheduleTimeText`/`introSentence` 等,故用 `git stash` 切回舊碼、`wrangler dev --remote` 接正式 D1,對四縣市各一個代表點人工比對修改前後的鄰近清運點清單):新北板橋(有座標)、台中北屯(無座標)、高雄阿蓮(無座標)三點 5/5 完全相同;桃園八德(村里為 `null`,走擴大候選集)4/5 相同,舊碼真正最近的「雅典桂冠」(`TYN-BADE-E675ED971B`)因不在候選集內被下一近的點取代,幅度符合預期的取捨規模。**遠端操作**:先備份(`d1/backups/pre-migration004-20260825_1358.sql`,73,244+31,158 筆與備份前 D1 現況核對一致),`wrangler d1 execute --local` 對多語句檔案有本機限定的已知限制(`contains several transactions`,與 remote 無關),改逐條 `CREATE INDEX` 驗證語法後,直接對 `--remote` 執行 migration 004(純新增索引、無 BEGIN/COMMIT),執行後高雄/台中/桃園/新北四市筆數(19,435/19,986/7,152/26,671)與 migration 前逐一相符 | 已被索引的查詢不代表查詢本身便宜——`EXPLAIN QUERY PLAN` 只回答「有沒有用索引」,不回答「這次要讀幾列」,兩者是不同問題,LIMIT 陷阱與 covering index 對 DISTINCT 無效這兩個發現都源自於過早把「有 SEARCH USING INDEX」等同於「已經夠便宜」;任何靠限量(LIMIT)換取效能的設計,若限量條件本身不是索引可直接定位的等值/範圍鍵,就只是把「什麼時候會退化成全表掃描」這個問題藏起來,不是解決它;鄰近推薦類功能精準度與讀取成本的取捨屬於產品決策,不該由實作單方面決定,本次每一個取捨點(候選集上限數值、村里優先 vs 地理 bounding box、LIMIT 陷阱的修法選擇)都先回報實測數字讓 Jun 選,才動手 | Jun 拍板(修法優先順序、鄰近點精準度取捨接受範圍、migration 004 執行同意)+ 自主(診斷、程式實作、本機/遠端驗證、前後輸出人工比對)

2026-07-29 | **新北市 Phase B 上線:CITIES 註冊 + 廚餘維度(foodscraps_schedule)導入 + 正式部署**:接續 2026-07-28 的 Phase A(資料層,見上方條目),完成並上線城市註冊、D1 廚餘欄位管線、頁面渲染三塊。**B1/B2(commit `42fd203a`)**:`site/src/lib/data.ts` 的 `CITIES` 加入新北;`data-d1.ts`/`data-static.ts`/`pipeline/push_d1.py` 三處補齊 `foodscraps_schedule` 的讀取/發佈過濾/匯入邏輯(`data-static.ts` 的 publishable 判斷從「schedule 或 recycling_schedule 至少一個非空」擴充為三者至少一個非空,與 `push_d1.py` 的 `classify()` 保持一致);`[point].astro` 新增「廚餘回收時刻」區塊。**B5 人工抽查**(全量掃描 26,671 筆,非抽樣):廚餘與一般垃圾/資源回收到站時間在有重疊星期時 **0 筆不一致**(107,512+102,342 次逐筆比對),但廚餘星期不是一般垃圾星期子集的有 353 筆(1.3%),其中 20 筆(22 個星期-entry)的額外星期在 schedule 與 recycling_schedule 皆查無對應——「廚餘與一般垃圾由同一輛清潔車收運」這句話對這些點缺乏對照組、無法驗證,改為「廚餘收運到站時間多與上方一般垃圾／資源回收時刻表一致」,只斷言有資料佐證的「時間關聯」、不斷言無法驗證的「車輛/路線一致」。**B3 稽核發現的落差修正**(commit `8fd6ad49`):`content.ts` 的 `introSentence()`/`todaySummarySentence()` 原本「schedule 為空」分支只判斷 `recycling_schedule`,若某點只有 `foodscraps_schedule` 非空(schedule、recycling_schedule 皆空)會誤判成「完全無公開時刻資料」——目前新北資料尚無此形狀的實例(現有 2 筆「僅回收+廚餘」點的 `recycling_schedule` 都非空,不受影響),但比照既有純資源回收點分支補上對稱的純廚餘點分支,防禦未來資料出現這種形狀。**K–P 組合矩陣**(`content.test.ts`,35→49 個測試):K/L/M/N/O 取自真實新北資料(子集型態、多出星期由資源回收涵蓋、多出星期完全孤立、無 foodscraps 回歸測試、僅回收+廚餘),P/P2 為合成防禦性 fixture(對應上述落差修正的純廚餘點分支,目前無真實案例)。`npm run build` 與全部測試通過後 push 分支(不 merge main)。**遠端 D1**:先備份(`d1/backups/pre-foodscraps-migration-20260729_1205.sql`,46,573 筆),確認 migration 002 為裸語句(無 BEGIN/COMMIT)、只新增 `foodscraps_schedule` 欄位不動既有資料、索引隨重建表一併 `CREATE INDEX` 回來,執行後高雄/台中/桃園筆數(19,435/19,986/7,152)與 migration 前完全相同,`foodscraps_schedule IS NOT NULL` 為 0(符合預期,既有三市本無此維度)。接著匯入新北 26,671 筆(`pipeline/push_d1.py xinbei`,全數 publishable,零筆濾除),四市合計筆數與 29 區分布皆與 Phase A 資料層報告一致。**版本預覽**(`wrangler versions upload`,版本 `c2fa7505-456f-4062-805e-2e40a3a9c784`):首頁/四城市索引頁/新北 8 個行政區頁皆 200;有廚餘、無廚餘、O 類(僅回收+廚餘,無一般垃圾)三種點分別驗證內容正確,O 類同時顯示「僅提供資源回收收運」摘要句與「廚餘回收時刻」區塊(驗證 B3 修正生效);`sitemap-index.xml` 自動帶出 `sitemap-xinbei.xml`,網址數 26,701(=1 城市頁+29 行政區頁+26,671 清運點)與 D1 吻合;既有三市 district/point 頁零回歸;舊格式 301 導向與 `meinong/06852` 404 皆正常。取得 Jun 同意後記下回滾錨點(`62e9f980-cec4-45a4-9c69-82f55ab92ffb`,2026-07-28 404 頁面改善版)、`wrangler versions deploy` 切正式。部署後 `mengwaba.com` 對 `/trash/xinbei/` 首次請求出現與 2026-07-27/28 同款 Cloudflare edge 負向快取(短暫 404),數次重試後穩定為 200 HIT,非新版本問題;正式網域複驗項目與預覽階段相同,全數通過,無需回滾 | 「到站時間相同」與「同一輛清潔車收運」是兩個不同強度的斷言,前者有資料可驗證(0 不一致)、後者沒有(無車輛欄位),文案只能陳述前者,避免對 5,000+ 頁規模的內容做無法驗證的事實宣稱;純廚餘點分支的修正屬於「發現即補、不因目前資料無實例就跳過」的防禦性原則,與桃園/新北過往幾次「共用程式碼未涵蓋新資料形狀」的教訓(如 I1、鐵律 10)是同一類问题的提前補強;遠端操作維持「先備份→查驗 migration 語句→本機驗證過的流程原封不動搬到遠端→逐步驗證筆數→版本專屬預覽→明確同意→部署→複驗」全套流程,沒有跳步 | Jun 拍板(遠端 D1 執行同意、部署同意)+ 自主(B1-B5/B3 程式實作、K-P 測試撰寫、備份與遷移執行、版本預覽與部署驗證)

2026-08-26 | **D1 免費額度超額修復第一批,正式上線**:接續 2026-08-25 條目(版本專屬預覽 `ac847931-1e66-495a-8624-cef4f7870ad4` 已完整驗證通過),取得 Jun 同意後執行 `wrangler versions deploy` 切正式(`site/` 目錄下操作)。切換前先用 `wrangler deployments list` 確認當時 100% 流量版本為 `a2c2dd4b-328d-420d-8521-82cedff60470`(2026-08-10 上線版,commit `909c2cdb`),與既有回滾錨點記錄一致,設為本次回滾錨點。部署後對 `mengwaba.com` 重跑完整驗證清單(非僅狀態碼):四城市各一清運點頁(title/JSON-LD 皆正確)、四城市各一行政區頁(含「其他行政區」「里別」區塊)、首頁、`/trash/` 城市索引頁、`sitemap-index.xml`、四份城市 sitemap 皆 200;舊格式 point_id 301 導向(`kaohsiung/alian/04892` → `54CFBD5BEA`)、兩種 404(未知清運點、未知縣市)皆正確;新北板橋樣本點「鄰近清運點」區塊仍為 5 筆。首輪請求即全數通過,未觀察到 edge negative caching 造成的暫時性不一致,**無需回滾**。**順便釐清 Jun 提出的疑問**(新北 sitemap 顯示 26,701,先前記錄 26,671 是否為資料異動):對四城市直接查 D1 `COUNT(*)`/`COUNT(DISTINCT district_slug)` 核對,`sitemap 網址總數 = 1(城市索引)+ 行政區數 + 清運點數` 這條公式四城市全部精確吻合(高雄 19,472=1+36+19,435;台中 20,016=1+29+19,986;桃園 7,166=1+13+7,152;新北 26,701=1+29+26,671),且新北的 26,671 與 2026-07-29 條目記錄的清運點數完全相同——**確認非資料異動**,26,671 從一開始指的就是清運點筆數(不含城市索引與行政區頁),不是 sitemap 網址總數,兩個數字本來就該相差 30(=1+29),只是先前不同條目引用的口徑不同造成使用者誤以為對不上。**Step 3(6 小時後拉 D1 insights 回報總讀取量/主查詢占比/行政區頁占比)尚未執行**,需等流量累積,留待下次接手 | 鐵律 7/8/9 全套流程走完(版本專屬預覽驗證→明確同意→部署→正式網域複驗→有問題才回滾)未省略任何一步;數字對不上時先查證兩個數字的定義是否本來就不同,不要預設是資料異動 | Jun 拍板(部署同意)+ 自主(部署執行、正式網域驗證、sitemap 公式核對)


2026-08-28 | **行政區頁改回完全靜態預生成(D1 額度修復收尾)+ 補齊全站 404 + CLAUDE.md 流程補記**:接續 2026-08-25〜27 陸續拆掉行政區頁借用的整區/全縣市規模 D1 查詢(見上兩條目與 `692a1fb9`、`13ab45c8`),拆到最後一步:行政區頁全台合計是有限集合(現行四縣市 + 全台 22 縣市擴充後預估數百頁),不像清運點頁(數萬頁,必須 on-demand 才能繞開 Cloudflare 部署檔案數上限)。改回 `getStaticPaths` 於 build 時窮舉 city×district 組合、直接用既有 `loadCityPoints`(`data-static.ts`,城市索引頁已在用的同一套 build-time bundle 讀取模式)+ `groupByDistrict` 取資料,不再 on-demand 查 D1,該頁 D1 讀取額度歸零。連帶清掉三個此後不再有任何消費端的死碼:`data-d1.ts` 的 `loadDistrictFromD1`/`toDistrictGroup`/`loadCityDistrictList`、`scripts/build-district-index.ts`、`site/src/data/district-index.json`、`package.json` 的 `build:district-index`/`prebuild` 掛勾(`loadCityDistrictList` 昨天(`13ab45c8`)才新增,由於這次改回完全靜態後行政區頁本身在 `getStaticPaths` 階段就已經拿得到同城市全部行政區清單,不再需要這個中介檔案,一日之內即淘汰,如實記錄非做白工,是修法方向本身在收斂)。**Jun 交辦的三項收尾確認**:①**日期/時間依賴稽核**:逐一檢查行政區頁本身與其引用的 `computeDistrictStats`/`fetchedAtDateRange`/`groupByVillage`(`data.ts`)、`buildDistrictFaq`/`weekdaySummarySentence`/`weekdayDistributionCaveat`/`districtTimeDistributionSentence`/`districtNoteSentence`/`scheduleTimeText`(`content.ts`)、`breadcrumbJsonLd`(`seo.ts`)、`WeekdayBadge.astro`、`BaseLayout.astro`,確認皆不含 `new Date()`/`Date.now()`/`toLocaleDateString` 等當前時間邏輯——`fetchedAtDateRange` 顯示的「資料更新於」日期取自資料本身的 `fetched_at` 欄位(資料被抓取當下就已固定的事實),不是頁面渲染當下的時間,本就該凍結,不是問題。全站唯一真正依賴當前時間的邏輯是 `data.ts` 的 `todayWeekdayTaipei()`(供 `todayScheduleEntry()` 預設參數用)與 `content.ts` 的 `todaySummarySentence()`,但這兩者只被清運點頁(`[point].astro`)的「今天狀態摘要」功能引用,行政區頁完全不 import——清運點頁維持 on-demand(未受本次變動影響),每次請求時 `new Date()` 仍是即時值,不會凍結,故現況下无需修正,僅記錄稽核結論供未來若清運點頁也考慮靜態化時參照(屆時 `todaySummarySentence()` 會是必須拿掉或改造的第一個障礙)。②**404 狀態碼與客製內容**:稽核發現 repo 原本**沒有** `src/pages/404.astro`,完全查無路由的請求(如打錯字的網址、非本站網址結構)會落到 Astro 預設的空白 404,只有清運點頁([point].astro],靠自身 on-demand 萬用路由攔截並用 `NotFoundContent` 元件渲染)撐住;行政區頁改回靜態前,無效的 city/district 組合原本是靠 on-demand 分支回傳純文字 `Not Found`,也不是客製內容。新增全站層級 `src/pages/404.astro`(維持靜態預生成,渲染共用的 `NotFoundContent`),同時把 `NotFoundContent` 原本針對清運點寫死的文案(「找不到這個清運點」)改成路由無關的通用版(「找不到這個頁面」),因為現在這個元件同時服務「完全查無路由」與「清運點確實不存在」兩種情境。本機 `wrangler dev` + curl 逐項驗證(非僅讀程式碼):有效行政區頁 200(內容含正確 title/FAQ/資料更新日期,無「今天」字樣殘留)、有效城市下不存在的行政區 404+客製內容、不存在的城市 404+客製內容、完全不成型的路徑(如 `/whatever-nonsense`)404+客製內容,四種情形皆非 Astro 預設空白頁;另確認 `wrangler.jsonc` 的 `assets.not_found_handling: "none"`(2026-07-20 事故教訓,避免 Cloudflare 靜態資源層自作主張 SPA fallback)不影響這次驗證——未命中靜態檔案的請求本就會落到 Worker 本體,由 Astro 路由層(含新增的 404.astro)接手判斷,兩者不衝突。③**季度更新流程補記**:行政區頁改回靜態後,季度資料更新(fetch → normalize → validate → 推送 D1)流程多了一步——**推送 D1 完成後,還必須重新 `npm run build` + 部署,行政區頁才會反映新資料**,不像清運點頁下次請求就自動拿到 D1 最新內容;若漏掉這步,行政區頁會安靜停留在上一次 build 當下的舊資料,沒有任何錯誤訊息或狀態碼異常可供察覺。已補記進 CLAUDE.md「資料更新與機器角色」段落,列為季度流程的必要步驟,避免下次執行時遺漏。**本次變動已於 2026-08-28 部署上線,見下一條目** | 行政區頁數量是有限集合、清運點頁數量是天文數字,兩者對「該不該完全靜態化」的判斷基準本來就不同,D1 額度修復的收斂終點是把「數量有限、變動低頻」的頁面徹底移出 on-demand 查詢路徑,只把 on-demand 留給真正因數量而必須如此的清運點頁;404 與日期稽核屬於架構變動的例行安全檢查,不因為「這次只是把某頁改回靜態」而略過,靜態化最容易犯的錯誤正是把本該即時的內容意外凍結、或把本該接住的路由意外漏接 | Jun 拍板(交辦稽核範圍與收尾要求)+ 自主(靜態化實作、死碼清理、404 補齊、本機驗證、文件補記)
2026-08-28 | **行政區頁完全靜態化,正式上線**:接續上一條目(commit `d13bc99e`,版本專屬預覽 `7879f4e4-ba23-409a-94b2-6d5e63b5f58d` 已由 Jun 複驗通過)。取得 Jun 同意後執行部署:先用 `wrangler deployments list` 確認當時 100% 流量版本為 `64cc4bb3-dd4e-4488-a975-47e3d480353c`(2026-08-27 上線版,commit `13ab45c8`),與既有回滾錨點記錄一致,設為本次回滾錨點,`wrangler versions deploy 7879f4e4-ba23-409a-94b2-6d5e63b5f58d@100` 切正式。部署後對 `mengwaba.com` 重跑完整驗證清單:首頁、四城市索引頁皆 200;四城市各一行政區頁(苓雅、北區、八德、八里)皆 200,title/JSON-LD/常見問題皆正確,「其他行政區」筆數 35/28/12/28(與部署前預覽驗證數字一致);清運點頁(苓雅一點)200;舊格式 point_id 301 導向(`kaohsiung/alian/04892` → `54CFBD5BEA`)正常;三種 404(有效城市+無效行政區、無效城市、完全不成型路徑)皆真 404 狀態碼且顯示客製內容(搜尋框+縣市列表);`sitemap-index.xml` 與四份城市 sitemap 皆 200。首輪請求即全數通過,**無需回滾**。**下一步**:部署後至少等 6 小時待流量累積,拉 `wrangler d1 insights` 回報 24 小時總 rows read 與各查詢占比——本次修法後行政區頁 D1 讀取歸零,預期總讀取量應遠低於 2026-08-25 事故時的 304% 額度、也低於 2026-08-26 上線時的水準,僅剩清運點頁(`loadPointById`/`loadNearbyCandidates`)、301 查詢(`lookupLegacyRedirect`)兩類查詢貢獻讀取量 | 鐵律 7/8/9 全套流程走完(版本專屬預覽驗證→明確同意→部署→正式網域複驗→有問題才回滾)未省略任何一步;部署前的預覽驗證數字與部署後正式網域數字逐項比對一致,不只驗證狀態碼、也驗證內容數字未漂移 | Jun 拍板(部署同意)+ 自主(部署執行、正式網域驗證)

2026-08-28 | **行政區頁完全靜態化上線後 6.5 小時 D1 insights 複驗**:接續上一條目。`wrangler d1 insights mengwaba-trash-points --time-period=1d --json` 顯示過去 24 小時總計 4,534,604 rows read(佔免費額度 500 萬/天的 90.7%),乍看仍偏高,但這個 24 小時滾動視窗橫跨了三個版本(`ac847931` 直到 08/27 10:24 UTC、`64cc4bb3` 直到 08/28 01:48 UTC、`7879f4e4` 目前這版),其中 `SELECT DISTINCT district, district_slug FROM points WHERE city_slug = ?`(901,219 rows、46 次,平均每次 19,591 列——已於 2026-08-27 拆除的舊查詢)與行政區頁的 `SELECT * FROM points WHERE city_slug = ? AND district_slug = ?`(126,382 rows、118 次——`7879f4e4` 已完全移除)合計 1,027,601 rows(占這 24 小時總量 22.7%),全數是**舊版本留下的殘留**,不是現行版本產生的。改用 `--time-period=6h` 抓一個完全落在 `7879f4e4` 生命週期內的乾淨視窗(部署至今 6.58 小時):**查無任何行政區頁形狀的查詢**,只剩四類——`loadNearbyCandidates` 擴大候選集(456,474 rows/1,744 次/平均 261)、`loadNearbyCandidates` 同村里候選集(359,675 rows/7,259 次/平均 49)、`loadPointById`(7,030 rows/7,030 次/平均 1)、`lookupLegacyRedirect` 301 查詢(2 rows/104 次)——與 2026-08-25 條目記錄的清運點頁查詢形狀完全吻合,**行政區頁 D1 讀取確認歸零**。乾淨視窗總計 823,181 rows,線性外推 24 小時約 3,004,259 rows(**約占免費額度 60.1%**)——比 Jun 預期的「遠低於額度」保守,如實回報不美化:比修法前(24h 90.7%,含已淘汰的行政區查詢殘留)確實下降,但下降幅度主要來自拆除行政區頁查詢(貢獻 22.7% 的那塊),清運點頁「鄰近清運點」查詢(`loadNearbyCandidates` 兩類合計占乾淨視窗 99.1%)才是現在的主要讀取來源,額度尚有餘裕但非「遠低於」,若城市數持續擴充(六都→全台 22 縣市),鄰近清運點查詢量會隨清運點總數等比放大,值得留意。**另發現一個本次修法範圍外、但同樣屬於全城市規模低效查詢的既有端點**:`sitemap-[city].xml.ts` 的兩支查詢——`SELECT MAX(fetched_at) ... WHERE city_slug=?`(cache key 版本號用)與 `SELECT point_id, district_slug, fetched_at FROM points WHERE city_slug=?`(組 sitemap URL 清單用)——平均每次讀約 2 萬列(`queryEfficiency` 僅約 0.00005),24 小時內共執行 15 次、貢獻 299,754 rows,乾淨 6.5 小時視窗內剛好 0 次觸發(低頻,推測隨搜尋引擎爬取 sitemap 的頻率而定,非穩定流量,乾淨視窗恰好沒觸發不代表之後也不會),與已修復的 DISTINCT 查詢是同一種反模式(為了一個小結果掃過整個城市範圍),**不在本次任務範圍內,不動它**,但如實記錄供下一批 D1 額度優化參考 | Jun 明確要求「有異常明確標出來不要略過」,故不因為「行政區頁確實歸零」這個好消息就淡化「額度用量並非遠低於預期」這個壞消息;24 小時滾動視窗天生會混入部署前的舊版本流量,量測架構變動後的效果必須改用完全落在新版本生命週期內的較短乾淨視窗,並如實標註是外推而非完整 24 小時實測 | Jun 拍板(要求複驗與如實回報異常)+ 自主(insights 查詢、新舊版本流量拆解、sitemap 端點問題發現)

2026-08-31 | **新工具「行事曆/連假查詢」Phase 0 資料源探勘,判定 Go**:選題理由與過去 pSEO 頁不同——8/31 GSC 證據顯示 Google 對新縣市行政區頁「已檢索但不建立索引」,同模板的高雄頁面卻有索引,差異只在高雄事故前累積過的網域信任度,瓶頸是網域信任而非頁面品質。這個工具的成功標準訂為「能否帶來第一批外部連結」,受益對象是含垃圾車七萬多頁在內的整個網域,故頁數刻意壓在 25 頁以內(全靜態、不碰 D1)。主資料源查證為政府資料開放平臺 `dataset/14718`(行政院人事行政總處,非另兩個舊版/地方版 dataset id),115(2026)/116(2027)年標準版 CSV(365 筆/年,西元日期+星期+是否放假+備註四欄)與 Google 行事曆專用版皆已確認可下載,授權為政府資料開放授權條款第 1 版。下載檔用「已知關鍵事實」(116 年 121 天放假、0 補班日、2/9-10 與 4/6 與 12/31 三個具體補假日)逐項交叉驗證,全數吻合,唯獨「3 天以上連假數」官方新聞稿正文自稱「共 9 個」但實際只列出 8 項,且與 CSV 實際算出的連假清單(逐一列出起訖日與天數)一一對應也是 8 個——回頭查證 DGPA 官方新聞稿原文(非搜尋摘要)確認新聞稿本文自身就只列 8 項,矛盾在官方文件本身,非本站解析錯誤。另查出 dgpa.gov.tw(實際檔案下載端點所在網域)robots.txt 全站對非 Google 爬蟲一律 `Disallow: /`,故拍板下載流程比照本專案既有「低頻、人在場」精神改成人工單次下載存檔,不寫自動化排程去打該網域。**Phase 1 拍板事項**:①連假數量頁面文案一律以 CSV 實際算出的清單為準(8 個,含起訖日與天數),不引用新聞稿的「9 個」;②補假辨識用備註欄位窮舉式白名單比對,遇未預期字串中止,不用寬鬆字串包含判斷;③農曆對照與節日白話說明本階段不做;④下載的原始 CSV(`data/raw/calendar/2026.csv`、`2027.csv`、`2027_google.csv`)從 /tmp scratchpad 複製進 repo 版控留存正本(`.gitignore` 沿用既有「`data/raw/*` 全擋、個別來源以 `!` 例外」模式新增一條),避免 /tmp 清空後須重新對外抓取 | 過去幾個縣市行政區頁反覆卡在「頁面本身沒問題但域名不被信任」,新工具若還是走同一套「靠頁面本身 SEO 取勝」的邏輯不會解決根本瓶頸,故這次刻意選一個「會被人主動分享」而非「會被人搜尋到」的形狀;連假數字取用官方文件裡兩個互相矛盾的數字時,選擇能被本站自己資料驗證重現的那一個(8,有逐筆明細可查),而不是選擇文字敘述但缺乏對應清單佐證的那一個(9),避免頁面上出現連自己都解釋不了、被讀者抓到矛盾的數字;dgpa.gov.tw 的 robots.txt 限制與本專案更新頻率(季度/年度、人工執行)天然合拍,不需要為了單次下載去踩灰色地帶 | Jun 拍板(選題、成功標準、Go/No-Go)+ 自主(資料源探勘、CSV 交叉驗證、官方新聞稿原文查核)

2026-09-01 | **行事曆工具 Phase 1:parser 完成並驗收通過,發現「端午/中秋不連假」不能跨年份共用文案**:`pipeline/parse_calendar.py` 解析 `data/raw/calendar/{2026,2027}.csv`,輸出逐日結構化資料至 `data/normalized/calendar/{year}.json`。六項驗證全過:①放假天數 2027=121、2026=120;②補班日兩年皆 0 筆(星期六/日仍為上班日的列數為 0,且「是否放假」欄位只允許 0/2,出現其他值會直接中止程式);③補假日 2/9、2/10、4/6、12/31 皆位於 2027、皆標註正確;④2027 年 3 天以上連假算出 8 個,逐項比對與 CSV 吻合(不是新聞稿的 9 個);⑤備註欄位窮舉 15 種字串,2026/2027 兩年皆完全命中、未觸發中止;⑥**端午/中秋/教師節是否形成連假,兩年結果不同**——2027 三者皆孤立、未形成連假(端午 06-09 週三、中秋 09-15 週三、教師節 09-28 週二);但 **2026 年端午(06-19 週五)因緊鄰週末形成 3 天連假,中秋+教師節(09-25~09-28)形成 4 天連假**,是 2026 曆表本身的事實,不是解析錯誤。**新訂原則:所有敘述性文案(連假清單、節日是否連假等判斷句)一律從當年度資料現算,不得寫死跨年份共用的句子**——「端午中秋不連假」這句話只在 2027 成立,2026 若照搬同一句話會是錯誤資訊,寫法上必須是「以 {{year}} 年資料計算」而非通用敘述 | 這與桃園垃圾車文案「沒有廚餘卻在文案裡寫廚餘」是同一類錯誤:把某一次資料觀察到的現象,誤當成跨資料集/跨時間都成立的通則寫進固定文案。行事曆這個題材换年就換一份新資料,尤其容易犯這個錯,必須在 Phase 1 就把「文案從資料現算、不寫死」定為原則,而不是等 Phase 2 頁面文案寫錯了才發現 | Jun 拍板(驗收 Phase 1、訂立跨年份文案原則)+ 自主(parser 實作、六項驗證、DECISIONS 記錄)

2026-09-01 | **行事曆工具 Phase 2(頁面 + .ics 訂閱端點)、Phase 3(請假攻略計算機)一次做完,尚未上線**:延續 Phase 1 的 parser 輸出(`data/normalized/calendar/{year}.json`),新增 `site/src/lib/calendar.ts` 純邏輯層(不碰 D1,build 時用 `import.meta.glob` 內嵌資料,比照 `data-static.ts` 既有模式)。**頁面結構**(Jun 選定「年度總覽 + 逐一連假頁」方案):`/calendar/`(工具首頁,列年度卡片)→ `/calendar/{year}/`(年度總覽:連假清單、端午/中秋/教師節逐年現算結果、請假攻略入口)→ `/calendar/{year}/{slug}/`(單一連假詳情頁,`slug` 取連假起始日 MMDD——同一年度連假區間不重疊,保證唯一,不用節日中文名稱當 slug 是避免翻譯/斷詞歧義)。**連假標題組字**:定義 `MAJOR_HOLIDAY_ORDER` 白名單(12 種正式節日名稱,排除「小年夜」「補假」等非節日本身的標記),從連假涵蓋的備註中挑出可辨識節日名稱組標題(如「兒童節・清明節連假」),查無白名單命中時退回「連假(起始日 起 N 天)」的日期敘述,不留空標題。**.ics 端點**(Jun 選定「build time 產生靜態檔」方案):`/calendar/{year}.ics`,只收錄備註非空的放假日(國定假日/補假,約 20 餘筆/年),不含純週末空白列——訂閱這份行事曆的人本來就知道週末,塞進去只是雜訊;`DTEND` 依 RFC5545 全天事件慣例算成「不含」的次日,已用 `file` 指令與逐項欄位比對確認產出合法 iCalendar。**訂閱而非下載**(Jun 二輪拍板追加):新增 `IcsSubscribe.astro` 元件,不再單純放 `<a href=".ics">` 連結(會被瀏覽器當檔案下載,存進 Downloads 資料夾後隔年不會更新、沒有回訪價值),改成 `webcal://` 連結給 iPhone/Mac 行事曆一鍵跳出訂閱對話框,另外用可選取的 `<code>` 純文字網址引導 Google 日曆網頁版「透過網址新增」流程(Google 日曆不支援 `webcal://` 一鍵訂閱,只能手動貼網址)。**Phase 3 請假攻略演算法**:`computeLeavePlans()` 找出「已成形的國定假日區塊」與前後緊鄰假期區塊(通常是純週末)之間 1~3 個工作日的缺口,提出拼假建議;**關鍵過濾條件是區塊必須含至少一個真正的國定假日**(備註非空且非「補假」)才能當錨點——最早的原型沒有這條限制,對任何兩個普通週末之間的一週工作日都會生出「請 4 天放 9 天」的提案(這句話對任何一週都成立,跟本站主題「國定假日」毫無關係,純粹是「請假一週」的廢話),加上這條限制後 2026 從 6 個假提案收斂為 1 個(僅元旦)、2027 從 9 個收斂為 5 個(春節、兒童節+清明節、端午、中秋、教師節),數字與已知的公開行事曆「拼假攻略」報導吻合(如 2027 小年夜前後請 5 天連放 16 天)。每個錨點只往前、往後各接一次緊鄰區塊(缺口 >3 個工作日不提案),不無限串接多個區塊。頁面 `/calendar/{year}/leave-plan/`(僅在 `computeLeavePlans().length > 0` 時才在年度總覽頁與 sitemap 中出現連結,2026/2027 皆有內容故兩年皆生成)。**驗證**:vitest 87 個測試全過(新增 13 個 calendar.ts 測試,含端午拼假案例、缺口過大不提案、純週末不錨定三種邊界情境的合成 fixture);`npm run build` 成功,產出 22 個 HTML 頁 + 2 個 `.ics`(共 24,在 Phase 0 拍板的 25 頁上限內);`astro preview` + curl 逐條驗證所有路由 200、`.ics` 回應 `Content-Type: text/calendar`、不存在的年份/slug 皆真 404 且顯示客製內容;`sitemap-static.xml` 已納入所有 calendar HTML 頁(`.ics` 不放進 sitemap,不是給搜尋引擎索引的頁面);品牌首頁 `/` 已加入這個工具的卡片。**本次僅完成本地實作與驗證,尚未 commit/deploy**,依鐵律 7/8 待 Jun 明確同意後才會走版本專屬預覽 → 正式網域的部署流程 | Phase 2/3 一起做是 Jun 明確要求(「做完一起上線」),但 Phase 3 演算法在動手前先用真實 2026/2027 資料跑原型驗證過兩次(先看到「任何一週都會生出提案」這個明顯不對勁的結果,才加上「必須錨定在真正國定假日」的限制),沒有寫完就直接信任第一版邏輯;.ics 訂閱與下載的差異看似是 UI 細節,但直接決定這個工具「能否帶來第一批外部連結」這個 Phase 0 訂下的成功指標是否成立——下載一次即用完的檔案不會被分享、不會被回訪,訂閱才有 | Jun 拍板(頁面結構選項、.ics 產生方式選項、訂閱優先於下載、Phase 2+3 合併範圍)+ 自主(演算法設計與原型驗證、頁面/元件實作、測試撰寫、本地建置與 curl 驗證)
